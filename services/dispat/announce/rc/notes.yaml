# What this release candidate says, written by a person: the headline, every
# link to this candidate, then the notes. rc/crier.yaml pulls this file in as
# its caption, so a new candidate is announced by editing this file and the
# same words on template.html, and never the configuration.
#
# The only value it takes from the release is {{ .new_version }}. The links come
# first so they survive a platform's caption limit. Discord refuses a message
# over 2000 characters and Instagram a caption over 2200, and crier posts a
# caption whole, so Discord gets the links and the lede and leaves the long
# paragraphs to the pictures attached to the same message, which carry them in
# full. `crier publish --dry-run --json` prints each platform's caption, which
# is how to check a rewrite against those limits.
--- |-
  {{ if eq .Platform "discord" }}@everyone {{ end }}dispat v{{ .new_version }} is ready for testing.
  This release candidate: https://github.com/yohimik/dispat/releases/tag/services/dispat/v{{ .new_version }}
  Linked repositories guide: https://dispat.dev/next/choreographed-repositories/
  dispat compute reference: https://dispat.dev/next/cli/compute/
  Report a problem: https://github.com/yohimik/dispat/issues

  Orchestration and choreography. No separate control repository.
  {{ if eq .Platform "discord" }}
  The full notes are on the pictures.{{ else }}
  Monorepo: release packages that live in one repository.

  Orchestration: start from one chosen repository to coordinate releases across linked repositories.

  Choreography: start a release from any linked repository.

  Both approaches to linked repositories work without a separate control repository. Each repository keeps its own configuration and release history.

  Connect repositories with a star, a minimal tree, or your own tree of links. `dispat compute --topology star` proposes direct links from the current repository to every other one. `dispat compute --topology minimal` keeps existing links and adds only what is needed. Custom links must connect the repositories without loops.

  Both commands preview changes. Add `--write` to apply them. Existing links are kept.

  Before planning, dispat locks every participating repository. If any lock fails, the release stops. It then calculates versions and releases dependencies before the packages that need them.

  If a library releases but an app using it fails, the successful release stays recorded. The next run plans the unfinished work without releasing the library again. This is saga recovery: keep what succeeded and finish what remains.

  Autistic stability for ADHD projects. Mathematical planning. Saga recovery.

  Check the plan with `dispat status`. It publishes nothing and takes no release locks.{{ end }}

  #golang #opensource #devtools #releaseengineering #cli #monorepo #polyrepo #devops #cicd #automation #softwaredevelopment
