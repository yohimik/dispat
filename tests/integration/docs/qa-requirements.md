# Release-candidate requirement matrix

This is the acceptance inventory for the release candidate. Its denominator is the behavior dispat implements: the
command tree and flags under `services/dispat/internal/cli`, the configuration model in
[`pkg/models`](../../../pkg/models/models.go) as the [configuration reference](../../../packages/docs/docs/configuration/README.md)
describes it, the current documentation under `packages/docs/docs`, and the parts of the
[CCME specification](../../../specs/ccme-spec/SPEC.md) dispat implements. It is not an inventory of test-plan headings: the
[test plan](./test-plan.md) says what each test proves, and this matrix says which released behavior that is evidence
for.

Every row states one requirement as a primary goal and one explicit behavioral assertion, and names the assertions that
demonstrate it. A reference is a globally unique `TestName` or a `path/to/file_test.go::TestName` qualified by the file
that declares it.

**Status is derived, not declared.** `go run ./tools/testreport testplan .` reads this file, resolves every reference
against the tree, and refuses a row whose status is not the one its references support:

- **mapped** means at least one named assertion exists.
- **uncovered** means no adequate assertion exists yet.

Whether a mapped assertion *passed* is the suite run's answer, recorded with its revision in the release evidence, not
something this file may claim on its own. A documented test is not evidence that it ran.

**Critical** marks a release-safety requirement: the ones where a defect publishes something wrong, republishes
something already out, loses the record of a publication that cannot be undone, or acts on a repository the operator
excluded. Locks, partial publication, records, cancellation, drift, duplicate publication, credential redaction and
repository exclusion are all in this set.

## Configuration

| ID | Critical | Goal | Assertion | Tests | Status |
|---|---|---|---|---|---|
| CFG-01 | no | Find the configuration | The file is found by ascending from the invocation folder to the repository root with no `--config`, and an unknown key stops the run before any work is done. | `TestConfigResolutionAscendsToTheMonorepoRoot`, `TestConfigResolutionAscendsPastASpaceFile`, `TestConfigUnknownKeyIsRejected`, `TestConfigUnknownKeyInsideASpaceIsRejected`, `TestConfigAbsoluteFileKeepsTheRequestedRepositoryRoot` | mapped |
| CFG-02 | no | The ladder from the root down | A setting written once at the root reaches every space and every standalone package, and each level below states otherwise, including an explicit `false` against an inherited `true`. | `TestLevelsRootFlowReachesEverySpace`, `TestLevelsRootReachesAStandalonePackage`, `TestLevelsRootBooleansAreThreeState`, `TestOverridesLadderNearestWins`, `TestOverridesInFolderFileWins` | mapped |
| CFG-03 | no | Merge rules per setting | `flow`, `scripts` and `env` merge entry by entry; `aliasTags`, `webhooks`, `buildOutputs`, `buildPlatforms` and `runOnly` replace whole; `dependencies` merge into one graph; `ignore` concatenates with `!` re-inclusion. | `TestOverridesScriptsAcrossTheLayers`, `TestOverridesSpaceLayerDependencies`, `TestWebhookPackageOverrideRouting`, `TestIgnoreScopeLevelsConcatenate`, `TestRecordsLineOverrideReplacesInherited`, `TestExecutionBuildOutputsLadder`, `TestExecutionRunOnlyLadder` | mapped |
| CFG-04 | no | Keys keep their case | A configuration key keeps the case it was written in all the way to the script environment. Two keys that genuinely fold together are refused with both named, including Unicode aliases that lowercasing separates; Unicode names outside the same fold cycle remain distinct. | `TestConfigNamesKeepTheirCaseEndToEnd`, `TestConfigRefusesTwoSpellingsOfOneName`, `TestConfigUnicodeSimpleFoldKeepsDistinctScriptNames`, `TestStaticEnvFromARefKeepsKeyCase`, `pkg/config/decode_test.go::TestDecodeRefusesTwoSpellingsOfOneKey`, `pkg/config/decode_test.go::TestDecodeKeepsDistinctUnicodeSimpleFoldNames`, `pkg/config/fold_test.go::TestFoldAgreesWithUnicodeSimpleFold` | mapped |
| CFG-05 | no | A configuration split across files | A `$ref` composes one value from one or more files in authored order, the keys beside the reference outrank every file it names, and a cycle or missing fragment stops the run before a tag, a commit or a script. | `TestConfigRefSplitsTheFile`, `TestConfigRefCycleFailsBeforeAnyWork`, `TestConfigRefMissingFragmentIsNamed`, `TestMultiRefMergesObjectFragments`, `TestMultiRefSiblingOverrideAndEnvCase`, `TestFinalRemainingImportedFolderConfigFailsClosed` | mapped |
| CFG-06 | no | Static environment layers | Root, space and package `env` maps merge with the most local winning, a `.env` file sits under both, and no static key may shadow a computed `DISPAT_` variable. | `TestStaticEnvReachesScripts`, `TestStaticEnvCannotShadowComputedVariables`, `TestDotenvReachesScriptsAndDispat` | mapped |
| CFG-07 | yes | Repository participation | `repositoryOverrides.<source>.enabled: false` removes the repository from the composed workspace before its checkout is inspected and before its history is initialized, so a source whose folder is absent still composes and contributes no packages. | `services/dispat/internal/config/workspace_hardening_test.go::TestComposeWorkspaceExcludesDisabledRepositoryBeforeCheckoutInspection` | mapped |
| CFG-08 | yes | Exclusion through a whole run | A disabled repository contributes no packages, hooks, baselines or remote release locks to a release run, while its paths keep their ownership boundaries for everything that is still enabled. | `TestPolyrepoExcludedRepositoryIsAbsentFromEveryPhase` runs status and release with an uninitialized excluded source and asserts its packages, scripts, baselines and remote lock are absent while a control package inside its path is refused; `TestPolyrepoExcludedProviderIsRefusedUnlessExternal` covers the dependency edge. | mapped |
| CFG-09 | yes | Repository ownership | Package identities are case-insensitively unique across sources, including Unicode case aliases; dependency endpoints resolve to the one canonical package. Central `.gitmodules` identities may use Unicode but cannot collide through an alias. An imported path cannot escape its owning repository, and the reserved `control` history cannot be shadowed by a `.gitmodules` name in any case. | `TestPolyrepoOwnershipValidation`, `TestPolyrepoRejectsReservedControlRepositoryName`, `TestPolyrepoRefusesASubmoduleInventoryItCannotRead`, `services/dispat/internal/config/packages_test.go::TestPackagesRejectUnicodeCaseFoldCollision`, `services/dispat/internal/config/packages_test.go::TestDependencyEndpointUsesUnicodeCaseFold`, `services/dispat/internal/config/workspace_test.go::TestComposeCentralWorkspaceRejectsUnicodeAliasSubmoduleIdentities`, `services/dispat/internal/config/workspace_test.go::TestComposeCentralWorkspaceFindsUnicodeAliasSubmodule`, `TestFleetWildcardScopesKeepEachSourceOwnIntent` | mapped |
| CFG-10 | no | Explicit composition only | `configs` and repeated `--configs` compose repository-local configurations from the paths they name, canonically equivalent paths compose one owner rather than duplicate identities, a root declaration wins over referenced declarations while preserving the winning path's own base, and a missing explicit import is fatal. | `TestPolyrepoImportedConfigsAndCLIImports`, `TestPolyrepoCanonicalConfigImportsAreDeduplicated`, `TestPolyrepoConfigImportResolvesFromDeclaringFragment`, `TestConfigImportReferencesRespectMergePrecedence`, `TestConfigNestedImportFragmentsKeepOriginsAndFailClosed`, `TestConfigImportFragmentCannotEscapeControlRoot` | mapped |
| CFG-11 | no | Folder exclusion | `.dispatexclude` hides a folder's packages, its space configuration file and its package configuration file from discovery, per folder of a multi-folder space. | `TestOverridesDispatexclude`, `TestConfigDispatexcludeSelectsTheConfigFile`, `services/dispat/internal/config/packages_test.go::TestDiscoverMultiPathExcludePerFolder` | mapped |
| CFG-12 | no | Configuration is refused, not repaired | A rejection in any layer lands before any work: a space-layer error, a record line with no text, an out-of-bounds `entrySpacing` and an unusable `env` key each exit 1 with the offending declaration named. | `TestConfigSpaceLayerRejections`, `TestRecordsLineWithoutTextIsAConfigError`, `TestRecordsEntrySpacingOutsideItsBoundsIsAConfigError`, `TestStaticEnvRefusesUnusableKeys` | mapped |
| CFG-13 | no | A fleet states who it is and who it links | A linked repository states its own identity and the roster of its peers; the walk composes that fleet from any peer with each peer's version groups local to that peer, refuses a second route between two repositories, an identity a link contradicts and a link nobody materialized, and reports a one-sided link or a roster gap without refusing the run. | `TestChoreographyComposesOneFleetFromAnyEntry`, `TestChoreographyRefusesASecondLinkPath`, `TestChoreographyRefusesAnIdentityItCannotTrust`, `TestChoreographyRefusesALinkNobodyMaterialized`, `TestChoreographyRefusesAFleetInventoryItCannotRead`, `TestChoreographyRefusesALinkPathThatIsNotARepository`, `TestChoreographyReportsAOneSidedLinkAndARosterGap`, `TestChoreographyRefusesTheKeysOnlyAControlRepositoryOwns`, `TestLinkedGroupsStayRepositoryLocal` | mapped |
| CFG-14 | no | Compute chooses repository link topology | Minimal preserves the existing graph and joins its groups at their centres, so the longest route between two repositories stays as short as the existing links allow, falling back to the member nearest a centre this run holds no checkout of; star links every peer to the entry and rejects conflicting links before writing. Invalid topology values never mutate the workspace. | `TestComputeStarUsesTheEntryAndRemainsStable`, `TestComputeStarRefusesRewiringBeforeWriting`, `TestComputeRejectsUnknownTopologyBeforeWriting`, `TestComputeTopologyExcludesDisabledRepositories`, `TestComputeTopologyRejectsCyclesWithoutChangingLinks`, `TestComputeMinimalJoinsExistingComponentsWithoutReplacingTheirLinks`, `TestComputeMinimalWritesTheJoiningLinkWhereACheckoutExists` | mapped |
| CFG-15 | yes | Execution settings are node-startup settings | `execution` belongs to the root configuration alone: a space, a package entry and a package folder's own file refuse it as an unknown key, an imported peer may carry its own and it is never consulted by a run started elsewhere, and a node that states nothing keeps the local release it had, with the same tag, the same run and no coordination branch on its remote. | `TestExecutionIsRefusedOutsideTheRootFile`, `TestExecutionInAnImportedConfigIsValidatedNotConsulted`, `TestExecutionAbsentKeepsLocalReleaseUnchanged` | mapped |
| CFG-16 | yes | Execution settings are refused at load | An unknown role, a capacity below one, a negative wait or ceiling, a node name that is not one, a worker that states workers, a link with no name or a duplicate name, a mailbox carrying credentials, a query, a fragment, a leading dash, a transport helper or an unauthenticated scheme, and worker links with no signing secret each exit 1 naming the key path and carrying E225, before any lock, plan or command; the refusal redacts the credential the address carried, and the accepted mailbox forms load unchanged, a link that states no endpoint among them. | `TestExecutionConfigRefusals`, `TestExecutionRefusalNeverPrintsACredential`, `TestExecutionRefusesAnUnreadableNumber`, `TestExecutionAcceptsEveryMailboxForm` | mapped |
| CFG-17 | no | A package declares what its build produces and where it may run | `buildOutputs` and `buildPlatforms` resolve through the ordinary ladder (repository, space, space folder file, package entry, package folder file), a nearer list replaces the inherited one whole and an explicit empty list opts out; each entry is held to a relative slash-separated path outside `.git` and to Go's `os/arch` spelling, and two packages claiming one folder are refused at discovery naming both packages. | `TestExecutionBuildOutputsLadder`, `TestExecutionBuildOutputRefusals`, `TestExecutionBuildOutputsInAPackageFolderFileAreRefusedToo`, `TestExecutionOverlappingBuildOutputsFailPreflight`, `TestExecutionBuildKeysAreAbsentFromAnOrdinaryRun` | mapped |
| CFG-18 | no | The repository root is a folder of the configuration | A space may be rooted at the repository, in which case its packages are the repository's top-level folders under the ordinary dot-folder and `.dispatexclude` rules and nothing may be listed beside it; a standalone `packages` entry may be rooted there too, in which case the package is the repository itself, records at the top, runs its scripts in the root and owns every file no deeper package owns. | `TestRootPathSpaceReleasesTopLevelFolders`, `TestRootPathPackageReleasesTheRepositoryItself`, `TestRootPathPackageOwnsOnlyWhatNoDeeperPackageOwns`, `TestRootPathPackageInALinkedPeer` | mapped |
| CFG-19 | no | A package rooted at the repository is held to what that folder can honour | The root configuration file is not read again as such a package's own in-folder layer, so the repository-wide keys it carries stay legal and the package entry remains the nearest word about the package; `revertOnFail` is refused there, written or inherited, because rolling that folder back would discard every other package's release files. | `TestRootPathPackageIgnoresItsOwnRootConfigAsAPackageLayer`, `TestRootPathPackageRefusals`, `services/dispat/internal/config/rootpackage_test.go::TestRootPackageDoesNotReadTheRootConfigAsItsOwnLayer` | mapped |
| CFG-20 | no | The provider relation is written as a boolean or as an object | `isBuildWaitingPublish` accepts `false` and `true` exactly as it always did and, beside them, an object naming `build` (`none`, `build` or `publish`) and `isBlocking`; `build` is required, the wait is matched exactly, an unknown key and `{build: publish, isBlocking: false}` are refused, and every refusal names the key on every level of the ladder that carries it, where a level stating the relation replaces it whole. | `TestStageRelationConfigRefusals`, `TestStageRelationLadder`, `TestPublicAPIModelStageRelationShapes`, `services/dispat/internal/config/stagerelation_test.go::TestStageRelationNearestLevelWins`, `services/dispat/internal/config/stagerelation_test.go::TestStageRelationLadderReplacesWhole` | mapped |
| CFG-21 | yes | Source commit validation preserves staged work | Invalid messages, unreadable Git validation settings and unusable original hook paths refuse a source commit without changing HEAD or the staged diff; a healthy retry accepts the same staged work once. | `TestCommitValidationBlocksInvalidMessageBeforeCommit`, `TestFinalCommitRefusesUnreadableRepositoryMetadataBeforeMutation`, `TestCommitRefusesAFileAtTheHooksPathAndRetries` | mapped |
| CFG-22 | no | The propagate stage's two names | `autoPropagate` is `autoVersion` and `flow.propagate`, `flow.beforePropagate` and `flow.postPropagate` are the version entries: either spelling releases the same way under the stage's runtime name, a nearer layer stating either replaces both, and one object stating both is refused before any work. | `TestConfigPropagateSynonymsInOneObjectAreRefused`, `TestAutoPropagateReleasesLikeAutoVersion` | mapped |

## Planning

| ID | Critical | Goal | Assertion | Tests | Status |
|---|---|---|---|---|---|
| PLN-01 | no | The release window | A never-released package's window is its whole history, and the first baseline tag under the configured `tagFormat` is what cuts the next window down. | `TestRecordsFirstReleaseCoversTheWholeHistory`, `TestPlanKeepsIndependentReleaseWindowsAfterMerge`, `TestFinalHistoryRetainsWorkWhenBoundariesLeaveHEAD`, `TestConfigInitialsBaselines`, `TestMergedIndependentHistoriesKeepTheirReleaseBoundaries` | mapped |
| PLN-02 | no | What counts as a change | Only a package's own files make a scopeless commit address it: a commit touching ignored files alone releases nothing and reports W131, and one ordinary file among them brings the package back. | `TestIgnoreScopeKeepsAFolderFromTriggeringARelease`, `TestIgnoreScopeDoesNotHideThePackage`, `TestPackagesSrcNarrowsChangeDetection` | mapped |
| PLN-03 | yes | Repository-scoped fatal errors | A dependency cycle (E200), duplicate version tags (E191) and a shallow clone (E196) abort the run with a non-zero exit, the code in the events, and nothing released or executed, whatever `commitErrors` says. | `TestFatalDependencyCycle`, `TestFatalDuplicateVersionTags`, `TestFatalShallowRepository`, `TestFatalErrorsIgnoreCommitErrors` | mapped |
| PLN-04 | no | Propagation to consumers | A provider release reaches its consumers at the configured bump and depth, and a catch-up entry spans the provider's movement since the consumer's own previous release rather than collapsing it. | `TestPlanTrainCatchUpStaysACatchUp`, `TestRecordsCatchUpGithubBodySpansTheProvidersMovement`, `TestRecordsCatchUpSpansEveryProviderOfAMultiScopeUnit` | mapped |
| PLN-05 | no | Exact pins | An exact `Release-As` pin is guarded against the channel and ordering rules, and a rejected pin falls back to the computed bump without swallowing a sibling unit's. | `TestPlanExactPinGuards`, `TestPlanRejectedPinFallsBackToTheComputedBump`, `TestEdgePinPrereleaseBelowTheBumpIsStillRefused` | mapped |
| PLN-06 | no | Selection narrows, never widens | `--package`, `--space` and `--group` narrow the plan; a Unicode alias selects its one canonical package and ambiguous package identities are refused before publication. A selected package whose provider is releasing unselected stays behind with W230, a partial versioning group reports W231, and `--strict` refuses either before anything is published. | `TestFilterNarrowsTheWindowNeverWidensIt`, `TestFilterStatusSelects`, `TestFilterReleaseRecordsOnlyWhatReleased`, `TestUnicodePackageSelectionRefusesAnAmbiguousRelease` | mapped |
| PLN-07 | no | The empty-plan gate | `--require-release` turns a correct plan that publishes nothing into exit 3, apart from exit 1's failures, and a package the plan holds back does not satisfy it. | `TestPlanRequireRelease` | mapped |
| PLN-08 | no | Corrections to the record | `Edits` restates and `Deletes` discards a record that no package has released yet, the newest correction of a target wins, and a correction of released work is a reported no-op rather than a silent one. | `TestCorrectionEditRestatesTheRecordBeforeRelease`, `TestCorrectionDiscardsWhatTheRecordPropagated`, `services/dispat/internal/plan/corrections_test.go::TestCorrectionReachingNoPackageStillReportsW209` | mapped |
| PLN-09 | yes | Cross-repository boundaries need evidence | A cross-repository consumer baseline is refused unless something proves which provider revision the consumer released against; a coincidentally matching gitlink and an opaque custom checkpoint message are both insufficient. | `TestPolyrepoCrossRepositoryBaselineRequiresEvidence`, `TestPolyrepoCustomCheckpointMessageProvesNoBaseline`, `TestPolyrepoImportedConsumerBaselineRequiresEvidence` | mapped |
| PLN-10 | yes | Control intent is projected, never assumed | Control-owned release intent is projected onto the active source history, and a source checkout too old to contain that intent is refused instead of being planned around. | `TestPolyrepoStatusRejectsFutureControlIntentForOlderSourceCheckout`, `TestPolyrepoIncomparableSourceDirectivesNeedCausalControlResolution` | mapped |
| PLN-11 | no | Histories stay repository-local | Two sources may carry the same commit object id and the same tag spelling without sharing windows, counters, channels or baselines. | `TestPolyrepoIdenticalObjectIDsAndTagNamesStayIsolated`, `TestPolyrepoPrereleaseAndStableWindowsStayRepositoryLocal`, `TestPolyrepoSameTagSpellingKeepsCheckpointOwnersSeparate` | mapped |
| PLN-12 | no | A fleet link is a boundary, not a change | A consumer's window in another repository starts at the revision its release recorded, one hop at a time along the one route between them, and a commit that only moved a link is not that package's own work. | `TestChoreographyReadsTheBoundaryFromTheReleasedLink`, `TestChoreographyRelaysTheBoundaryAlongTheRoute`, `TestChoreographyDoesNotCountALinkMoveAsAChange`, `TestChoreographySinceProjectsThroughTheFleet`, `TestChoreographySinceARevisionThatPinsNothing`, `TestChoreographyReadsEveryReleaseSubjectOfARepositoryAtOnce` | mapped |
| PLN-13 | no | An unprovable boundary is reported, never guessed | A tag the links cannot account for stops the plan with E333 naming the remedy rather than assuming a revision or failing inside Git, and the `repositoryBaselines` tuple the diagnostic names resolves it wherever the fleet declares it. | `TestChoreographyNeedsATupleWithoutAReleaseCommit`, `TestChoreographyReadsABaselineDeclaredByThePeerThatKnowsIt`, `TestChoreographyRefusesAnUnprovableBoundaryRatherThanGuessing`, `TestChoreographyIncomparableDirectivesStayE334` | mapped |
| PLN-14 | yes | Repository facts must be parseable | Malformed successful Git replies cannot erase pending commits, published baselines or repository ownership facts; status stops instead of constructing a partial plan. | `TestFinalPolyrepoMalformedSourceHistoryCannotShrinkThePendingWindow`, `TestFinalPolyrepoMalformedTagInventoryCannotEraseThePublishedBaseline`, `TestFinalPolyrepoMalformedRepositoryFactsFailClosed`, `TestFinalHistoryRefusesMalformedMergeBaseBeforePublishing` | mapped |
| PLN-15 | yes | A propagated bump is owed until it is delivered | A provider's unit is admitted for a dependent until a release of that provider carrying the commit is one the dependent's own release reached, read from tags and ancestry alone. A consumer that proceeded past a failed or held provider catches up at a bump above its own release, whether the provider publishes in the same run, in a later run the consumer sat out, or while the consumer is held or has failed; a provider publishing in a run the consumer sat out leaves the commit in the consumer's owed window, so the next run still finds the debt, a consumer's prerelease train that carried the commit without the provider's version is owed exactly as a stable release is, and a tag message carrying an rc.5 `dispat-seen-v1:` payload is read as an ordinary release tag. A delivery already made admits nothing, a consumer cancel can discard an owed pickup before publication, provider changes without propagation add no bump, a removed provider creates no debt, and a Unicode alias in a dependency converges on the next run. | `TestAdmissionCatchesUpAConsumerThatProceededOnItsOwn`, `TestAdmissionCatchesUpAfterTheProviderShipsAlone`, `TestAdmissionCatchesUpAHeldConsumerAfterItsProviderShipped`, `TestAdmissionCatchesUpAConsumerThatFailedAfterItsProviderShipped`, `TestAdmissionCatchesUpAConsumerThatProceededAtALaterCommit`, `TestAdmissionReadsTagsCarryingAReceiptAsOrdinaryReleases`, `TestAdmissionNestedTagCatchesUpAfterTheProviderShipsAlone`, `TestAdmissionCatchesUpFromAnUnreleasedProvider`, `TestAdmissionCancelAfterProviderShipsAlone`, `TestAdmissionProviderAloneDoesNotCreateAConsumerRelease`, `TestAdmissionRemovedProviderCreatesNoDebt`, `TestAdmissionCatchesUpAConsumerThatOvertookAHeldProvider`, `TestAdmissionOwesATrainConsumerThatProceededPastItsProvider`, `TestAdmissionLeavesPlansWithoutOvertakingUnchanged`, `TestUnicodeDependencyAliasConvergesOnTheNextRun`, `services/dispat/internal/plan/owed_window_test.go::TestOwedWindowsPlanAsTheWholeHistoryDoes` | mapped |
| PLN-16 | yes | Two releases on one commit are never left to ancestry | A run that would release a provider at the baseline commit of a consumer it still owes, without releasing that consumer after it, is refused with E201 before any hook, build or publish, naming the consumer, the provider, the commit and both remedies, while `status` prints the same error and exits 0; releasing the consumer in the same run, or the provider after a new commit, delivers the debt. A consumer that fails after its provider published on the consumer's own commit is reported as a critical E201 naming the exact `Release-As` that delivers it, and committing that footer ships the consumer. A standalone `dispat commit --tag` that would tag such a provider alone is refused the same way before any commit or tag. | `TestAdmissionRefusesAProviderReleasedAloneAtItsConsumersCommit`, `TestAdmissionOwesATrainConsumerThatProceededPastItsProvider`, `TestAdmissionReportsAConsumerThatFailedAfterItsProviderAtItsCommit`, `TestAdmissionNestedTagCatchesUpAfterTheProviderShipsAlone`, `TestStandaloneCommitTagRefusesAProviderAtItsConsumersCommit`, `services/dispat/internal/plan/owed_at_head_test.go::TestOwedAtHeadReportsAProviderReleasedAloneAtItsConsumersCommit` | mapped |

## Versioning

| ID | Critical | Goal | Assertion | Tests | Status |
|---|---|---|---|---|---|
| VER-01 | no | The seven modes | `independent`, the full-version `fixed`/`fixedSparse` and the partial `fixedMajorMinor`/`fixedMajor` pairs plan side by side across several runs without bleeding into one another. | `TestVersioningAllModesSideBySide`, `TestVersioningFixedSpaceLifecycle`, `TestVersioningFixedMajorMinorSparseLifecycle` | mapped |
| VER-02 | no | Declared groups across spaces | A `versionGroups` group holds one version across spaces, a member that states its own `versioning` detaches, and members may spell their tags differently under that one version. | `TestVersionGroupMemberOverrideLeavesTheGroup`, `TestVersionGroupDivergentTagFormats`, `TestVersionGroupPartialSparseAcrossSpaces` | mapped |
| VER-03 | no | Shared prerelease trains | A group's prerelease train advances on one counter, invalid counter or channels policy is refused, a channel conflict inside it reports W236, and a propagated graduation transition graduates the whole train rather than abandoning dependants. | `TestVersioningFixedSharedPrereleaseTrain`, `TestVersionGroupPrereleaseTrain`, `TestVersionGroupRefusesUnknownSharingAxes`, `TestPlanPropagatedGraduationTransitionGraduatesTheTrain` | mapped |
| VER-04 | no | A failed ride catches up | A member that failed its ride catches up on the stable line and mid-train, a member owed a provider's release keeps the version it was planned at when its catch-up fails or sits a selection out, and every member's scripts run for a ride it takes. | `TestVersioningFixedRideFailureMidTrainHealsOntoTheTrain`, `TestVersioningFixedMajorRideFailureThenAlignment`, `TestVersionGroupOwedCatchUpKeepsThePlannedVersion`, `TestVersioningRideExecutesEveryMemberScript` | mapped |
| VER-05 | no | Versioning `none` | A `none` package is never versioned, tagged, changelogged or published, still runs its scripts on pending changes, and is refused as a provider to a releasable package at config load. | `TestVersioningNoneLifecycle`, `TestVersioningNoneProviderEdgeRejected`, `TestVersioningNoneReleaseOnlySettingsInert` | mapped |
| VER-06 | no | Tag names and aliases | `tagFormat` renders per level, and an alias is a pointer at a release rather than a record of one: an alias that cannot be written is W232 and the release tag is unaffected. | `TestRecordsAliasTags`, `TestRecordsAliasTagFailureIsOnlyAWarning`, `TestConfigFusedPrereleaseTagFormatRoundTrips`, `TestAliasPrereleaseShapesStayOutsideReleaseHistory` | mapped |
| VER-07 | yes | No duplicate publication of a recorded version | A checkout stale enough to re-plan a version whose record the remote holds is stopped by reading the remote's tags first: the run names the tag and leaves the published ref where its own release put it. A publication whose record never landed is not this requirement's case: VER-09 states its contract. | `TestReleaseRefusesToRepublishAnExistingTag`, `TestRecordsPushRefusesEveryPackageWhenOneIsAlreadyRecorded` | mapped |
| VER-08 | no | Manifests reach the planned version | Native auto-versioning reconciles ranges and own-version fields at the version stage under the configured policy, and the space's `syncLock` scripts run serialized where the manifests changed. | `TestAutoVersionSyncLockSerialised`, `TestAutoVersionPolicyFlagsStillRunSyncLock`, `TestStandaloneAutoversionReconcilesAndSyncLocks` | mapped |
| VER-09 | yes | A publication without its record is published again | A publish whose tag or record never landed leaves nothing that says the version is out, so a retry uploads it again unless the publish script verifies its destination, as the agent guide documents; the retry then establishes the missing record once. | `TestFinalDeferredTagWriteFaultRetainsTheReleaseCommit`, `TestFinalOrchestratedSourceCommitFaultNeedsAReviewedRetry`, `TestFinalSourceChangeAfterPublicationLeavesNoFalseRecord` | mapped |
| VER-10 | no | The sign stage owns the own version | With `autoSign` enabled the sign stage writes each package's own version before the propagate stage, which then writes dependency ranges and replace rules alone, the standalone `dispat autoversion` included unless `--write-version` asks otherwise. | `TestAutoSignWritesTheOwnVersionBeforePropagate`, `TestAutoSignStandaloneAutoversionWritesRangesOnly` | mapped |

## Execution

| ID | Critical | Goal | Assertion | Tests | Status |
|---|---|---|---|---|---|
| EXE-01 | no | Dependency order | Scripts run in the order the graph dictates under both `isBuildWaitingPublish` settings, a diamond converges, and a provider failure skips the waiting consumer rather than building it. | `TestOrderChainRunsInTopologicalOrder`, `TestOrderBuildWaitsForPublishWhenConfigured`, `TestOrderProviderFailureSkipsTheWaitingConsumer` | mapped |
| EXE-02 | no | Concurrency budgets | With concurrency 4 and five packages the fifth's work starts only after one of the first four finishes, independent packages are picked up concurrently, and the publish budget is independent of the build budget. | `TestConcurrencyBuildBudgetEnforced`, `TestConcurrencyIndependentPickedUpConcurrentlyDependantAwaited`, `TestConcurrencyPublishBudgetIsIndependentOfBuild` | mapped |
| EXE-03 | no | The script frames | Every stage sits inside a frame of hooks that nest, and the authority split holds: a hook before the point of no return may fail its package, one after it may only warn. | `TestHooksAllStageHooksFireInOrder`, `TestHooksRunLevelHookFailureSemantics`, `TestHooksScriptOutputsCarryAcrossStagesAndHooks` | mapped |
| EXE-04 | no | The login gate | `flow.login` runs once per space in the space folder, and a login failure reaches that space alone. | `TestHooksLoginOncePerSpaceAcrossSpaces`, `TestHooksLoginFailureIsolatedToItsSpace` | mapped |
| EXE-05 | yes | Cancellation is not failure | A SIGINT mid-run kills the in-flight script through the real signal handler, reports the remaining packages as `cancelled` rather than `failed` or `skipped`, tags nothing for work that did not finish, and the next run releases them at the version they were owed. | `TestInterruptGracefulShutdown`, `TestInterruptStopsARunCommand`, `TestWebhookInterruptedRunStillReportsTheOutcome` | mapped |
| EXE-06 | yes | Cancellation during recording | A SIGINT inside a source commit hook stops the later commit and push hooks while preserving the source tag, the source push, the control checkpoint and the control push that had already succeeded. | `TestPolyrepoInterruptionDuringRecordKeepsNativeRecordsAndSkipsHooks` | mapped |
| EXE-07 | yes | Existing work is protected | A dirty tree refuses a release only where a rollback could overwrite it: a package whose effective `revertOnFail` is false keeps the operator's work instead of losing it. | `TestEdgeDirtyGuardProtectsOnlyPackagesThatCanBeReverted`, `TestEdgeRevertOnFailStopsAtThePublish`, `TestEdgeRevertOnFailNeverReachesAFailedCommit` | mapped |
| EXE-08 | no | The release guards | `run.allowBranch` and the push-mode behind-remote check refuse the run before the plan exists, and both are off unless asked for. | `TestGuardAllowBranch`, `TestGuardBehindRemote`, `TestGuardsAreUnsetByDefault` | mapped |
| EXE-09 | no | Scripts run somewhere definite | `dispat exec` resolves one declared script against its subject and runs it where it is told, `dispat if` and `dispat for` stay transparent in a pipeline, and every helper's exit code is the chosen script's. An interrupted loop stops remaining items even with `--keep-going` and runs its one failure cleanup. | `TestForCancellationStopsRemainingItemsAndRunsCleanup`, `TestExecResolvesTheSubjectsScriptThroughTheBinary`, `TestIfPropagatesTheExitCode`, `TestForOverNothingSucceedsUnlessItemsAreRequired`, `TestLinkedSpaceExecUsesEntrySettings`, `TestFleetSpaceCommandsResolvePeerOwner`, `TestFleetSpaceLoopsAndCurrentFolderKeepDistinctOwners`, `TestFleetSpaceCommandRefusesAmbiguousPeerOwners`, `TestFleetSpaceCommandDoesNotReadUnselectedPeerFolder`, `TestFleetNestedHelpersKeepTheirCurrentPackageSelection` | mapped |
| EXE-10 | no | Nested steps inside a run | A stage script may run `dispat changelog` and `dispat commit --tag` inside the run it belongs to; finalize then finds the work done (W223/W226) and a second run converges. | `TestStandaloneStepsInsideAReleaseFlow`, `TestStepsWiredIntoAPublishLeg` | mapped |
| EXE-11 | yes | Nested work stays inside its owner | A nested step reads the live pin of the exact source commit it just made, masks only its own owner's in-flight tags, and a foreign owner's export cannot authorize an unpinned checkout (E330). | `TestPolyrepoNestedStepsReadLiveCommitPin`, `TestPolyrepoNestedStepMasksTagsWithinItsOwner`, `TestPolyrepoNestedForeignOwnerExportCannotAuthorizeSourceHead` | mapped |
| EXE-12 | yes | A worker never initiates a release | A node whose `execution.role` is `worker` exits 1 with E226 and the `execution-authority` class before the release lock is pushed, holding no remote lock and writing no tag, and still answers `dispat status` on the same configuration. | `TestExecutionWorkerRoleRefusesRelease` | mapped |
| EXE-13 | yes | Worker authority refuses native release writes | Under the worker-authority marker the bare release, `release`, `commit --tag --push`, `github`, `changelog`, `autoversion` and `compute` each exit 1 with E226 and write no tag or commit, including when a declared build script invokes one, while `status`, `exec`, `if` and `trigger` stay allowed so a build can still run and still report its progress. | `TestExecutionWorkerAuthorityRefusesNativeRefWrites`, `TestExecutionNestedCommandInheritsWorkerAuthority` | mapped |
| EXE-14 | yes | A delegating run refuses what it could not coordinate | With worker links configured, the environment kill switch, a configured `unsafeDisableLock`, a linked peer stating one, and a `secretEnv` variable that is unset or empty each exit 1 with E225 and the `execution-configuration` class before any lock push and with no branch in the mailbox; with an empty worker list the bypass stays the W331 warning it always was, and with the lock on and the secret set the run releases. | `TestExecutionRefusesUnsafeLockBypass`, `TestExecutionMissingSecretRefusesDistributedRelease` | mapped |
| EXE-15 | no | Execution settings are the entry's alone at release time | An imported source of a control repository and a peer of a linked fleet may each state `execution`: the entry's release writes exactly the tags it writes without them and reports one debug line naming each repository whose settings it ignored. | `TestExecutionLinkedPeerExecutionKeysAreIgnored` | mapped |
| EXE-16 | no | The plan a distributed run fixes | A run with worker links names its plan once, and the name covers the semantic plan alone: the number, names, order and capacity of the workers, the checkout's path and the time of the run leave it unchanged, while a new commit, a changed build command or a changed `buildOutputs` changes it, and a run with no links neither computes nor reports one; if the final source HEAD read fails, status, release and run refuse before naming a fixed plan or dispatching work, with a healthy retry able to plan. | `TestExecutionPlanDigestIgnoresPlacementAndTime`, `TestExecutionPlanDigestRefusesUnreadableHead`, `TestExecutionPlanIsIndependentOfWorkers` | mapped |
| EXE-17 | yes | A node acts only on work it can authenticate | A serving node answers a probe addressed to it and refuses everything else with a reason and no echo of what it refused: another secret's signature, an authentic document signed as another kind of message, an unsigned tip, another node's work, a message bound to another branch, another protocol version, a message outside the replay window, an oversized document, an illegal chain and a triple it already answered in an earlier process. A signed publication assignment without a publish permit is refused before claim; one without a package, frame or repository input closure is claimed and reported failed without running any script, while the node continues serving. | `TestExecutionWorkerAnswersProbe`, `TestExecutionWorkerRejectsAssignments`, `TestExecutionWorkerLeavesWorkItCannotRun`, `TestExecutionWorkerFailsIncompletePublicationFrames` | mapped |
| EXE-18 | no | A serving node starts and stops on its own terms | `dispat worker` names every setting it needs before it starts (E225), refuses worker authority (E226), keeps its process id in one state-lock file whose claims settle before they are trusted so simultaneous processes cannot share its answered-work record, stops with E225 when another process's id replaces its own, takes over a crashed holder, retains answered work for the full 48 hours a future-issued accepted message can remain valid, prunes older answers while still serving, and exits 0 on signal and idle timeout; a deleted object cache and a one-time git failure cost one fetch rather than the process, with Git's automatic fetch maintenance attached to that fetch rather than detached behind it. | `TestExecutionWorkerStartRefusals`, `TestExecutionWorkerConcurrentStaleStateClaimHasOneOwner`, `TestExecutionWorkerStopsWhenAnotherProcessClaimsItsFolder`, `TestExecutionWorkerPrunesAnsweredWorkWhileServing`, `TestExecutionWorkerKeepsFutureIssuedReplayKnowledge`, `TestExecutionWorkerStopsOnSignal`, `TestExecutionWorkerSurvivesALostStateDir`, `TestExecutionWorkerGitFaults`, `services/dispat/internal/gitx/maintenance_test.go::TestFetchKeepsAutomaticMaintenanceAttached` | mapped |
| EXE-19 | yes | A pool is validated before anything is dispatched | A release that delegates work refuses with E225 before any stage script runs, with no tag, the lock released and owned branches cleaned, when a node does not answer, cannot authenticate the run, cannot persist its answered-work record, would transfer less than the run requires, satisfies no releasing package's platforms, when the orchestrator's own git fails around the mailbox, or when a reply cannot be authenticated; a signed probe claim without a result becomes cleanup authority, while a claim bound to another work kind or a report bound to another work kind, attempt, plan or ownership remains unchanged for investigation; a signed malformed Claim or Result and a Result after Go are refused without adopting their mailbox tips; an authentic report with an unsupported protocol or zero capacity is refused before dispatch and its owned branch is cleaned; a healthy node is reported ready and the release completes. The public execution resolver fills absent bounds but leaves explicit invalid values visible for named loader rejection. | `TestExecutionPreflightRefusesBeforeAnyStage`, `TestExecutionPreflightPassesThenReleasesLocally`, `TestExecutionPreflightIgnoresUnauthenticReplies`, `TestExecutionPreflightRejectsMalformedAndOutOfSequenceReplies`, `TestExecutionPreflightRefusesAuthenticUnusableNodeReports`, `services/dispat/internal/execution/coordinator_test.go::TestPreflightClaimWithoutReportKeepsOnlyAuthenticatedCleanupOwnership`, `services/dispat/internal/execution/coordinator_test.go::TestPreflightResultRequiresAClaim`, `TestExecutionMailboxGitFaults`, `TestPublicAPIModelExecutionResolversPreserveExplicitBounds` | mapped |
| EXE-20 | no | Transport leftovers never fail a release | A coordination branch a finished run could not delete is reported with W244 and the `transport-cleanup` class while the release still tags its package and exits 0, because a coordination branch carries no release record. | `TestExecutionRetainedBranchIsAWarning` | mapped |
| EXE-21 | yes | Build stages execute on the nodes that were given them | With worker links configured every build frame of a release runs on a worker node and none on the orchestrator, from a prepared input state that carries the working tree as the orchestrator had it including the version stage's own edits; the release still writes its tags at the planned head, and preparing those states moves no head, writes no branch and stages nothing in the repository's own index. | `TestExecutionBuildsRunOnWorkersFromThePreparedSnapshot`, `TestExecutionMissingCommittedIndexCannotPublish`, `TestExecutionTasksSharingARepositoryUseSeparateBranches`, `TestExecutionConcurrentPackageNamesKeepSeparateWorkerCheckouts`, `TestExecutionTransportCommitsStayOutsideReleaseHistory` | mapped |
| EXE-22 | no | Delegation multiplies no budget | The run-wide build budget bounds a delegating run whatever the pool underneath it is, a node's own capacity bounds that node across runs, and the shared lock-file preparation stays on the orchestrator with every dispatched build reading the state it left. | `TestExecutionBudgetsHoldAcrossNodes`, `TestExecutionWorkerCapacityHoldsAcrossRuns`, `TestExecutionLockfilePreparationRunsOnTheOrchestratorOnly` | mapped |
| EXE-23 | yes | A dispatched failure is the package's failure, here | A node reporting a failed hook or script fails that package at its build stage on the orchestrator, runs its `onFail` script there, leaves the package untagged and leaves the same node healthy enough to build the rest of the run; a node that stops answering within `timeouts.task` keeps its slot, leaves the pool with E227, publishes nothing and gives the lock back; and a build that wrote outside what it declared is W244 with the orchestrator's own checkout unchanged. | `TestExecutionRemoteBuildFailureFailsThePackageLocally`, `TestExecutionTaskTimeoutLeaksTheSlot`, `TestExecutionStrayWritesAreReported`, `TestExecutionSnapshotGitFaults`, `TestExecutionWorkerTaskGitFaults`, `TestExecutionLostClaimPushIsRecognized` | mapped |
| EXE-24 | yes | A task carries worker authority wherever it runs | Every command of a dispatched task runs under the worker-authority marker, so a build script invoking `dispat release` or `dispat commit --tag` is refused with E226 on the node and the package fails at its build stage with nothing tagged; and a node configured as an orchestrator serves the work addressed to it without consulting its own worker links. | `TestExecutionWorkerTaskRefusesNestedRelease`, `TestExecutionOrchestratorRoleNodeServesDelegatedTask` | mapped |
| EXE-25 | yes | Declared build outputs reach the machines that need them | A provider's git-ignored build output folder travels to every consumer of it: the provider builds once on one node, its consumers build on other nodes from the exact bytes it produced without rebuilding it, a consumer whose mailbox is not the producer's reads a relayed copy, and the orchestrator's own checkout ends with every releasing package's declared roots installed so the stages that stay there see the files. Every kind of file a build produces travels through the Git transport alone: an ignored file, an executable, an empty file, names with a newline or outside ASCII, a relative link inside its root and an eight mebibyte file, out of two sibling roots. A root that is itself a file replaces an older tracked file, reaches dependent builds and is visible to local publication. A linked worktree on another filesystem from its Git index still installs and publishes those bytes atomically without admitting staging to Git status or the tagged tree. | `TestExecutionIgnoredOutputsReachConsumersOnOtherWorkers`, `TestExecutionOutputsRelayAcrossDedicatedMailboxes`, `TestExecutionBranchTransportCarriesEveryDeclaredOutput`, `TestExecutionFileOutputRootReplacesAFileAndFeedsConsumers`, `TestExecutionLinkedWorktreeInstallsOutputsAcrossFilesystemBoundary` | mapped |
| EXE-26 | yes | An output set that cannot be verified fails the prerequisite | Missing, defective or incompatible outputs fail the prerequisite they belong to and are never substituted: a build that did not write a declared root fails its own package with E227 and no consumer of it gets as far as reading its outputs, a real producer's named pipe or nested Git repository inside a declared root is refused before publication, a crafted manifest breaking any path, link, mode, agreement, ceiling, platform, ownership or digest rule is refused with E227 and its own reason enum, an unsigned result is refused on the authority code, and a reference whose bytes were taken off the mailbox makes the consumer run no command at all. A two-root set whose second root has a blocking file, linked parent or write-denied destination restores the first root to its previous bytes, writes nothing outside the checkout, leaves no temporary root beside it, and cannot publish the producing package; after permissions are repaired, a later run publishes it once while independent work remains usable. The release obligation survives: a later plan still names both packages. | `TestExecutionDefectiveOutputFailsThePrerequisite`, `TestExecutionCraftedOutputSetsNeverReachAConsumer`, `TestExecutionUnsignedOutputsNeverReachAConsumer`, `TestExecutionPartialOutputSetStaysUnready`, `TestExecutionOutputGitFaults`, `TestExecutionProducerRefusesNamedPipeOutput`, `TestExecutionProducerRefusesNestedRepositoryOutput`, `TestExecutionIncompleteHashReplyCannotPublish`, `TestExecutionRelayFailureFailsTheConsumer`, `TestExecutionOversizedManifestIsRefusedWhereItIsWritten`, `TestExecutionOutputInstallRollsBackEarlierRoots`, `TestExecutionOutputInstallPermissionFailureCanRetry`, `TestExecutionMalformedObjectStreamCannotPublish`, `TestExecutionMalformedTreeStreamCannotPublish`, `TestExecutionMalformedMailboxListingCannotStartRelease`, `TestExecutionInvalidMessageSizeCannotStartRelease` | mapped |
| EXE-27 | yes | An admitted output set is read at the object it was admitted at | A completed build publishes one admitted output set for its exact attempt rather than a moving directory: a garbage commit pushed on top of the producer's branch after its result was admitted changes nothing for the consumer, which builds from the bytes the run accepted. | `TestExecutionMovedBranchTipIsNotFollowed` | mapped |
| EXE-28 | no | A provider relation orders the builds | What a consumer's version and build stage waits for on a changed provider is the provider's relation: nothing under `none`, so the provider and its consumers build at once; the provider's build under `build`, which may still overlap its publish; and the provider's publish under `publish`. Build order is taken over the whole dependency graph, so a provider reached through a package with nothing to release still builds first however many such routes lead to it, while a package reached only across a `none` hop is not ordered at all. | `TestStageRelationBuildsOverlapWhilePublishesFollowTheProvider`, `TestStageRelationBooleansKeepTheirMeaning`, `tests/integration/stage_relation_test.go::TestStageRelationOrdersBuildsThroughAPackageThatDoesNotBuild`, `TestOrderBuildWaitsForPublishWhenConfigured`, `TestOrderBuildDoesNotWaitForPublishByDefault` | mapped |
| EXE-29 | yes | Where a stage may run is the operator's to state | `runOnly` rides the ordinary configuration ladder and replaces as a `[build, publish]` pair: a package pinned to the orchestrator is built there and nowhere else, a stage pinned to a worker is never run on the orchestrator however much capacity it has, and a pinned build that fails fails its own package here with its `onFail` script and names no worker. A stage that may only run on a worker, in a configuration declaring no `execution.workers`, refuses the release with E225 naming the package and the stage before any stage script runs; every shape and word the key does not have, and a publish pinned to a worker beside a space that logs in, are refused as the configuration is read. | `TestExecutionRunOnlyLadder`, `TestExecutionRunOnlyPerStagePair`, `TestExecutionWorkerOnlyBuildNeverRunsOnTheOrchestrator`, `TestExecutionWorkerOnlyWithoutWorkersRefusesTheRelease`, `TestExecutionRunOnlyRefusals`, `TestExecutionOrchestratorBuildFailureIsALocalFailure`, `TestExecutionALoginKeepsThePublishHere`, `TestExecutionOrchestratorOnlyBuildIsNotHeldToTheWorkersPlatforms`, `TestPublicAPIModelRunOnlyPreservesStagePlacement` | mapped |
| EXE-30 | yes | A build the orchestrator keeps is a build like any other | A frame placed on the machine the release was started on captures, describes and admits its declared outputs through the path a worker's result travels, so consumers on other nodes receive exactly the bytes it produced over a relay branch, and a pinned consumer reads the outputs its providers produced on workers from this checkout. Under the default that machine is one more node of the pool and the node of last resort: it takes a frame only when no worker has room, the worker is preferred while it is free, and the run's own build budget bounds every node together. With no `execution` object the key changes nothing a reader sees. | `TestExecutionOrchestratorOnlyBuildFeedsWorkers`, `TestExecutionOrchestratorOnlyBuildConsumesWorkerOutputs`, `TestExecutionBothUsesTheOrchestratorWhenWorkersAreFull`, `TestExecutionBothPrefersAFreeWorker`, `TestExecutionRunOnlyIsInertWithoutWorkers`, `TestExecutionOrchestratorOnlyBuildOwesItsOutputsToo` | mapped |
| EXE-31 | yes | A build dependency the run does not release still reaches its consumer | A provider whose package needs no new release is built once per run and its declared outputs are handed to every consumer of it, while nothing of a release is invented for it: no version, no tag, no changelog file, no delivered event and no entry in the fixed plan, whose digest is the one `dispat status` printed before the run. A provider that could not be built fails every consumer of it at the build stage with E227 naming it, is not attempted again, and leaves unrelated packages releasing. A build-only edge the propagation kinds exclude supplies bytes exactly as a runtime edge does, a provider reached only across a `none` relation is not built at all, and a cyclic graph is refused with E200 before anything is dispatched. | `TestExecutionUnselectedProviderIsPreparedNotReleased`, `TestExecutionPreparedProviderFailureBlocksItsConsumers`, `TestExecutionDevDependencyOutputReachesConsumer`, `TestExecutionDependencyCycleFailsBeforeDispatch`, `TestExecutionPreparationLeavesThePlanDigestUnchanged`, `TestExecutionAPreparedBuildIsToldItIsNotReleasing`, `TestExecutionPreparedProviderObeysItsOwnPlacement`, `TestExecutionPreparedProviderObeysItsOwnPlatforms`, `TestExecutionAPreparationThatFailedHereIsTheConsumersFailure`, `tests/integration/execution_prepare_test.go::TestStageRelationNoneNeedsNoPreparation` | mapped |
| EXE-32 | yes | Local bytes never replace a registry | Transferring a provider's build outputs to its consumer leaves the registry prerequisite exactly where it was: under the publish relation a consumer's build starts only after the provider's publication ended and still receives the provider's declared bytes, and a consumer whose provider never published is never placed on any node, runs nowhere, is reported blocked with W194 and tags nothing. A `none` relation carries no bytes at all and still orders the publications. | `TestExecutionRegistryEdgeStillWaitsForPublication`, `TestExecutionRegistryEdgeWithdrawsAConsumerWhosePublicationFailed`, `tests/integration/execution_prepare_test.go::TestStageRelationNoneCarriesNoBuildOutputs` | mapped |
| EXE-33 | yes | Workers may be added to every history mode | A single repository, a control repository whose sources are submodules and a fleet of linked peers each plan and release the same way with and without worker links: the same packages at the same versions, the same plan name for one link and for three, the same tags in the repository that owns each package, the same files in every commit the release added, the control checkpoint and the fleet-link settlement a run without workers leaves, no transport commit in any history and no coordination branch left behind. A consumer whose provider lives in another repository builds on another machine from the exact bytes that machine produced, through the path the provider's folder has on the orchestrator. | `TestExecutionModesPlanIsIndependentOfWorkers`, `TestExecutionModesReleaseAsTheirTwinWithoutWorkers`, `TestExecutionFleetBuildSeesProviderOutputsAcrossRepositories` | mapped |
| EXE-34 | yes | One entry acquires the complete lock set | Two entry points of one fleet addressing overlapping repository sets: the one holding the shared repository keeps its locks and completes, and the other is refused with E336, gives back the lock it had already taken, fixes no plan, dispatches nothing and releases nothing. | `TestExecutionConcurrentEntriesOneAcquiresAllLocks` | mapped |
| EXE-35 | yes | The signing secret stays where it is | In every history mode the secret reaches neither process's output and none of the objects the mailbox holds, read object by object rather than through the branches a finished run deletes. | `TestExecutionSecretNeverReachesAnyMailboxObject` | mapped |
| EXE-36 | yes | A failed git call stops exactly the work it belonged to | Every git invocation a dispatch and a report are made of, failed one at a time on the machine that issues it: a message that can never be read refuses the release before a stage runs, an input state, an assignment or a preparation that cannot be prepared or offered fails its package with E227 and publishes nothing, a read of a node's answer that fails once costs one poll, and a checkout record that cannot be pruned is a warning the release survives. The node carries on serving in every row. | `TestExecutionOrchestratorGitFaults`, `TestExecutionWorkerTransportGitFaults` | mapped |
| EXE-37 | yes | A source tag survives a checkpoint that fails | A delegating run that published a source and then could not write the control repository's checkpoint of it fails with E335, keeps the source's tag and published status, publishes nothing else, leaves the consumers of that source for the next run and still closes its coordination branches. | `TestExecutionControlCheckpointFailureKeepsSourceSuccess` | mapped |
| EXE-38 | yes | A delegated publication starts only under an explicit authorization | A publish stage the operator delegated runs on a node up to the irreversible command and stops there: the node installs the package's own verified build outputs, runs the `beforePublish` hook and reports itself ready, and exactly one single-use authorization is issued per publication, after the run has revalidated its ownership and the package's inputs, so no node's publish stage ever starts before it was authorized. Everything a release is made of is still written on the machine that started it, at the head the plan was computed against. A run that lost its lock withdraws the waiting node instead, with the lock code, and no command runs. Failure to commit or offer the publish input snapshot or relay the build's own output stops before authorization, closes refs and locks, and permits a healthy retry. Each message of the handshake failing on its own fails the release with nothing recorded. If no authorization arrives after ready, the worker ends the wait on the assignment deadline, reports failure, and never runs publish. | `TestExecutionPublishRunsOnWorkersUnderAuthorization`, `TestExecutionUnauthorizedPublisherNeverStarts`, `TestExecutionRemotePublishFailureKeepsLocalSemantics`, `TestExecutionPublishHandshakeGitFaults`, `TestExecutionPublishInputPreparationFaults`, `TestExecutionPublishExportsReachTheRecorders`, `TestExecutionPublisherWithoutGoEndsAtItsOwnDeadline` | mapped |
| EXE-39 | yes | Publication credentials stay on the machine that holds them | A publish is delegated only where the operator asked for it in so many words: the default and a space that configures a login both publish on the machine the release was started on, and no publish assignment reaches a mailbox at all in either case. A delegated publish expands the configuration's declared pairs on the node, from the node's own environment, so a token present only there reaches the command; a value present only in the orchestrator's environment appears in no mailbox object and in neither process's log at trace level. | `TestExecutionPublishStaysOnTheOrchestratorByDefault`, `TestExecutionPublishWithALoginStaysOnTheOrchestrator`, `TestExecutionPublishCredentialsResolveOnTheWorker`, `TestExecutionSecretNeverReachesMailboxOrLogs` | mapped |
| EXE-40 | yes | A publication is withheld when what it was built from moved | The revalidation behind an authorization is narrow and is made again for every publication: a commit made during the run inside the publishing package's folder or a provider's folder withholds dependent publications with E227 and runs their commands nowhere, while the same commit outside every folder the builds read changes nothing, because a release writes into its own repository constantly. Builds of independent packages still overlap on two machines while the publications of one repository never overlap. A denied permit whose withdrawal is not acknowledged marks the worker unhealthy without inventing an unknown publication or retaining the release lock. | `TestExecutionUnacknowledgedWithdrawalBeforeAuthorization`, `TestExecutionRelevantNativeChangeWithholdsPublication`, `TestExecutionChangedProviderWithholdsDependentPublications`, `TestExecutionUnrelatedNativeChangeKeepsResult`, `TestExecutionBuildsOverlapWhilePublicationSerializesPerOwner` | mapped |
| EXE-41 | yes | A queued assignment is not a lost node | A node's capacity is the node's, so two runs sharing one node of capacity one make the second run's assignment wait in its mailbox, and a task deadline that is longer than one build and shorter than two still completes both runs: the queue is not charged as run time, the node's concurrency stays at one, and no node is marked unhealthy for having been busy with somebody else's work. A claim that wins the assignment revocation race is reconsidered immediately and can publish exactly once. | `TestExecutionPublishClaimWinsQueuedRevocation`, `TestExecutionQueuedWorkIsNotATimeout`, `TestExecutionWorkerCapacityHoldsAcrossRuns` | mapped |
| EXE-42 | no | A run's own transport branches are not work | A prepared input state or a relayed result that a run pushed into a node's own namespace is skipped before it is read: the delegated build still runs on that node, and the node reports no rejected assignment and no authority warning for a branch the run itself created. | `TestExecutionTransportBranchesAreNotRejectedAssignments` | mapped |
| EXE-43 | yes | A receipt binds to one attempt of one run | The orchestrator believes a node's reply only when its signature, its header and every identity it echoes are this attempt's: the run, the work kind, the plan digest, the task, the attempt, the ownership generation, the node, the branch and the exact assignment object. A reply failing any of them, including a complete receipt of an earlier run, is ignored with one reason and nothing it contained, admits no output set, tags no version and leaves the attempt to fail on its own deadline; a correctly bound reply is accepted. A forged, malformed, expired, wrong-kind or replayed claim likewise grants no progress toward work. A forged, wrong-kind or unsupported-protocol ready after a valid claim grants no publication authorization. Cancellation and cleanup preserve the exact rejected claim or ready tip for investigation. | `TestExecutionStaleReceiptIsRejected`, `TestExecutionOldRunReceiptGrantsNothing`, `TestExecutionForgedClaimsAreRejectedBeforeWork`, `TestExecutionForgedReadyCannotAuthorizePublication` | mapped |
| EXE-44 | yes | An unaccountable publication is reported as neither published nor failed | A publisher that was authorized and never reported leaves an outcome nobody can establish: the package fails at publish with E228 and no record, its command ran exactly once, no second attempt is authorized in that run, the run exits non-zero, the summary counts the outcome apart from published and failed, and the exclusion covering that repository stays on its remote with a remedy naming the order of recovery. A result push whose answer was merely lost is recognised instead by re-reading the branch and is neither repeated nor reported as unknown. An authentic acknowledgement that the authorized command never started makes its outcome known and releases the lock; an acknowledgement signed by another key, bound to another cancellation or work kind, malformed or using an unsupported protocol cannot prove quiescence. A lost withdrawal response is resolved from its authenticated remote commit without stacking another cancellation; an unanswered publisher retains its lock and branch. A lock lost mid-run withholds every later effect with E336 and is neither re-created nor deleted by the run that lost it. | `TestExecutionLostPublishReplyIsNeverRepublished`, `TestExecutionLostWithdrawalPushResponseRetainsUnknownAuthorization`, `TestExecutionAuthorizedWithdrawalNeedsItsOwnAcknowledgement`, `TestExecutionLostResultPushIsRecognizedNotRepeated`, `TestExecutionLockLossStopsNewEffects` | mapped |
| EXE-45 | yes | A node that stops answering costs capacity, never correctness | A release interrupted mid-build withdraws the attempt, re-reads the branch when its lease is refused, waits for the node to say nothing of it is running, and reports the package cancelled rather than failed, with no failure hook and nothing recorded; the node keeps serving. A node killed mid-build fails its package, keeps its slot, leaves the pool with a named reason, has no dependent of that package placed anywhere, publishes nothing, leaves no publication in doubt and still releases every exclusion, because a compute attempt holds no authorization and is fenced by revoking its ref. A cancellation whose lease loses to a Ready signed by another party or a Cancel naming another run never retries atop that foreign tip; the branch is retained while the ordinary release lock is released. If a real worker finishes a build but cancellation wins the Result push lease, it acknowledges the cancellation rather than retrying its Result, so the interrupted run releases its lock and cleans the branch; a signed Cancel for another run, a malformed signed Cancel or one using an unsupported protocol that wins the same lease is rejected with its exact reason and left untouched without an Ack. Interruption during a publisher’s preparation withdraws it without an authorization or unknown effect. | `TestExecutionInterruptedPublisherBeforeAuthorization`, `TestExecutionInterruptCancelsRemoteTasks`, `TestExecutionTerminalMessageRacesWithdrawalLease`, `TestExecutionWithdrawalRefusesForeignPredecessors`, `TestExecutionWorkerAcknowledgesWithdrawalThatBeatsItsResult`, `TestExecutionWorkerRefusesForeignWithdrawalAfterResultLeaseLoss`, `services/dispat/internal/execution/cancel_reread_test.go::TestWithdrawalSettlesAResultAfterTwoLeaseLosses`, `services/dispat/internal/execution/cancel_reread_test.go::TestWithdrawalOfUnclaimedAssignmentNeedsNoWorkerAcknowledgement`, `TestExecutionKilledWorkerBlocksDependentsAndKeepsCapacity`, `TestExecutionUnclaimedPublicationCanRetry` | mapped |
| EXE-46 | yes | Transport state is never a recovery ledger | What a distributed run leaves on a mailbox or in a node's cache holds no part of a release: a batched deletion that cannot be pushed still publishes and tags the package, names the retained refs in a warning and exits zero; a build receipt with a failed publication records nothing and the next plan owes the same version; deleting a node's state folder, the private fetched refs and every coordination branch after a failed run leaves the same version owed and rebuildable, and after a completed run leaves nothing planned at all. | `TestExecutionCleanupFailureKeepsPublishedOutcome`, `TestExecutionTaskReceiptDischargesNothing`, `TestExecutionPrepublicationFailureRemainsRebuildable`, `TestExecutionDeletedTransportStateLeavesRecordsAuthoritative` | mapped |
| EXE-47 | yes | Only the run's own authorization starts a publication | A node waiting at the publication gate acts on one message and refuses every other: an authorization signed with another secret or not signed at all, signed over another message kind or naming another work kind, unparseable, expired, stating no expiry, addressed to another node, bound to another branch, of another protocol, or naming another ready, assignment, run, attempt, ownership or plan. Each refusal names the rule and echoes nothing the message contained, and no publish command runs for any of them although every attempt had already run its `beforePublish` hook; a message satisfying all of them starts the command once and the attempt reports success. A withdrawal is held to the same rules, so one nobody signed and one naming an earlier tip stop nothing, while a valid one is acknowledged as the attempt's terminal message with the phase it was in and no command started. A signed Ready that beats the run’s initial withdrawal lease is authenticated before Cancel is retried on top, and an Ack of that exact Cancel releases the lock. A branch moved or deleted after the node has authenticated authorization but before its last remote check withholds the command. | `TestExecutionCraftedPublicationAuthorizations`, `TestExecutionCraftedPublicationAuthorizationStartsTheCommand`, `TestExecutionCraftedPublicationWithdrawals`, `TestExecutionWithdrawalRetriesAfterReadyMovesTheLease`, `TestExecutionPublicationFenceSeesRemoteWithdrawal` | mapped |
| EXE-48 | yes | An exclusion is given back only on evidence that the publisher stopped | The acknowledgement of an authorized publisher, and nothing else, decides whether a run may report a clean cleanup. A run interrupted while a node is inside its publish command reports E228 with the `publication-unknown` class, is told by the node that its command had begun, and still gives the release lock back because a publisher that answered has provably stopped. A node that can no longer write to its own branch answers neither its result nor the withdrawal, so the same class is reported with `quiesced` false and the release lock of the repository it was publishing into stays on the remote. A publication refused before it was authorized is neither unknown nor retained: the waiting node is withdrawn with E336, answers from the gate that nothing started, and its command runs nowhere. In a fleet the rule is narrower still: one lock is retained per unaccountable publisher and no others, so a peer that published normally in the same run gets its lock back and its version recorded. The coordination branch of an unknown publication remains available for reconciliation while unrelated refs are cleaned. A lost authorization response is settled by reading the branch: an authorization still on it, or under the result the node wrote, landed and the publication is recorded, while one that is no longer there remains unknown even if the branch subsequently disappears or is reset, because a worker may already have consumed it. An authorization push the remote refused never reached the branch, so it withdraws the waiting publisher and gives the lock back without E228. A result the node wrote before the withdrawal could be is what became of the publication: a success is recorded and a failure is an ordinary failure, while an acknowledgement saying the command started, with no result, stays unknown. | `TestExecutionInterruptedPublisherIsQuiescedAndGivesTheLockBack`, `TestExecutionUnansweredPublisherRetainsTheLock`, `TestExecutionLostAuthorizationResponseRetainsExclusion`, `TestExecutionRefusedAuthorizationPushWithdrawsThePublisher`, `TestExecutionLockLostDuringTheHookWithdrawsTheWaitingPublisher`, `TestExecutionFleetRetainsOnlyTheUnansweredRepositoryLock`, `services/dispat/internal/execution/publish_test.go::TestARefusedAuthorizationPushIsNoUnknownPublication`, `services/dispat/internal/execution/cancel_reread_test.go::TestWithdrawalKeepsWhatAPublisherReported` | mapped |
| EXE-49 | yes | A path or an entry a consumer could not safely install is refused by name | The description of an output set is held to what a path, an entry and a link may be before a single file is installed: unsorted entries, totals the entries do not add up to, an entry that is neither a file nor a link, a mode no build product carries, an executable symlink, a negative length, a null byte or a colon in a path, an empty or dot component, a path under no declared root, a symlink pointing at nothing or at a target with a null byte or a backslash, and a tree whose type or mode disagrees with the manifest. Each refusal names the rule and never the path, nothing is admitted, the producing package fails with E227, and a reply larger than the configured manifest ceiling is refused as oversize before it is parsed at all. | `TestExecutionOutputEntryRulesRefuseTheSet`, `TestExecutionOversizedManifestIsRefusedBeforeItIsRead` | mapped |
| EXE-50 | no | A pool with no capacity refuses rather than waits | An assignment no node claimed within the run's own wait is revoked rather than abandoned: nothing was executed anywhere, so the slot is free, the node is not marked unhealthy and the task is offered again, twice and no more. The third refusal carries E227 and says the pool had no capacity for the task rather than anything about the package, no build of it ran anywhere, nothing of it is recorded, and the run occupying the node releases normally while the node keeps serving. | `TestExecutionQueuedAssignmentIsReplacedAndThenRefused` | mapped |
| EXE-51 | yes | A named output set is bytes or it is a refused prerequisite | An assignment naming a provider's outputs is believed only when the object it names is a signed result of this run whose manifest is the one the run admitted and whose tree holds the files it describes: no digest, an object carrying no result, no branch, a result describing no outputs, one with no signature, one signed with another secret, one that is not JSON, a digest that is not the manifest's, another ownership, and a file the tree does not hold are each reported as a failed `inputs` part with the rule's own word and run no command at all. Two inputs naming one branch are fetched once, installed into both folders the assignment names, and the frame then runs against them. | `TestExecutionCraftedInputsFailThePrerequisite`, `TestExecutionCraftedInputsInstallOnce` | mapped |
| EXE-52 | no | A node serves only with a whole memory of what it answered | A node refuses to start when its own folder cannot be created, when its lock cannot be read, when a live legacy PID still owns that lock, when legacy lock content is oversized, and when the record of answered work is unreadable or is not JSON: each says why it cannot serve, none starts, and none reads the mailbox. An invocation naming no folder keeps that state under the cache directory of the account it runs as, in a folder named after the node. If the record becomes unwritable while serving, a claimed task does not start, and repairing the record lets the same node serve new work. | `TestExecutionWorkerStateRefusals`, `TestExecutionWorkerKeepsItsStateUnderTheCacheDirectory`, `TestExecutionWorkerRefusesWorkWhenItsReplayRecordCannotBeSaved`, `TestExecutionClaimWithoutDurableReplayRecordCannotPublish` | mapped |
| EXE-53 | yes | A lost exclusion is decided once and remembered | Ownership is checked against the remote before every assignment and publication authorization: a build that takes the release lock off the remote makes the next task decide the loss and refuse, and every task after it is refused from that decision without asking again. In a fleet, a local publication is withheld when the beforePublish hook removes a different participating repository’s lock. The loss is reported exactly once however many tasks it stops, only the build that took the lock away ran anywhere, nothing is recorded, no publication is authorized, and the lock is neither re-created nor deleted by the run that lost it. A read of the lock that fails is bounded and read again, up to three reads, before the run counts the lock as unverified and stops as it does for a loss, giving back the lock it still owns. Ownership is asked before the first probe too, so a run that lost its lock while it planned offers no node anything. | `TestExecutionLostLockIsRememberedForEveryLaterTask`, `TestExecutionFleetPeerLockLossWithholdsLocalPublication`, `TestExecutionOwnershipReadIsRetriedBeforeALoss`, `services/dispat/internal/release/lock_verify_test.go::TestLockVerifyHeldRetriesAFailedRead`, `services/dispat/internal/release/lock_verify_test.go::TestLockVerifyHeldBoundsAHungRead`, `services/dispat/internal/execution/coordinator_test.go::TestPreflightRefusesALostLockBeforeAnyProbe` | mapped |
| EXE-54 | yes | A sweep that reaches other machines is refused where a release is, and holds no lock | `dispat run` with worker links is refused under worker authority and on a worker node with E226, and beside a lock bypass or with no signing secret with E225, before any task runs; a sweep that runs pushes no release lock and runs while a release holds one, leaving that lock as it found it. | `TestExecutionRunWorkerFlagRefusals`, `TestExecutionRunSweepRunsWhileAReleaseHoldsTheLock`, `TestExecutionRunSweepPlacesScriptsOnWorkers` | mapped |
| EXE-55 | no | A sweep's tasks are placed like builds and carry their exports | With worker links each package's task of a sweep runs where its `runOnly` places a build, the orchestrator last under `both`: a provider's export reaches a consumer on another machine, a task pinned to the orchestrator stays here, a task pinned to a worker with no link is refused with E225, a task that does not report within `timeouts.task` fails with E227 and its dependents are skipped, and the summary names the node, the computation and the exports of every task. | `TestExecutionRunSweepPlacesScriptsOnWorkers`, `TestExecutionRunSweepUnderRunOnlyOrchestratorStaysLocal`, `TestExecutionRunSweepUnderRunOnlyWorkerWithoutLinksIsRefused`, `TestExecutionRunSweepTaskTimesOutLikeABuild`, `TestExecutionRunSweepFailuresAreThePackagesOwn`, `TestExecutionRunSweepRefusesAPoolThatDoesNotAnswer` | mapped |
| EXE-56 | no | A sweep with no worker link is the sweep it always was | With no `execution` object and with one naming no worker, a sweep runs every task here, writes the same lines in the same order, fixes no plan, prints no summary, pushes no lock and writes nothing to the remote. | `TestExecutionRunSweepWithoutLinksIsLocalAndLockless` | mapped |
| EXE-57 | no | A pipeline names the worker it created on the command line | `--worker name` or `--worker name=endpoint` on release, run and status adds a link held to every rule a configured one is, a value that is neither is a usage error, is refused with E226 under worker authority or on a worker node, is no part of the plan digest, and on `status` names the plan without reaching the node. | `TestExecutionRunWorkerFlagNamesAMachineTheFileDoesNot`, `TestExecutionRunWorkerFlagRefusals` | mapped |
| EXE-58 | yes | What a sweep's tasks write comes back merged, never settled by arrival order | `runOutputs` roots written on other machines are verified and merged into the orchestrator's checkout file by file, keeping what no task wrote; a root a script did not write is an empty set; two tasks writing one path with different bytes fail the sweep with E227 and the root holds neither file; a crafted set is refused by the one validator; a set whose second file cannot be written into the checkout restores its earlier file, leaves no temporary names and fails the sweep with E227; an interrupted sweep merges nothing; and a root that is or holds a package folder in the entry checkout or linked fleet, overlaps a build output root or is stated below the root file is refused before any sweep runs. | `TestExecutionRunOutputsMergeIntoTheOrchestratorsCheckout`, `TestExecutionRunOutputsConflictLeavesNeitherFile`, `TestExecutionRunOutputsCraftedRefusals`, `TestExecutionRunOutputsConfigRefusals`, `TestExecutionFleetRunOutputsRefuseASourcePackageFolder`, `TestExecutionRunOutputsInASpaceAreUnknown`, `TestExecutionRunOutputsMergeFailureFailsTheSweep`, `TestExecutionRunInterruptedSweepMergesNothing` | mapped |
| EXE-59 | yes | A pick-up is the same release wherever it runs | The whole owed pick-up, from a consumer proceeding past its failed provider through the E201 refusal of a provider-only retry at the consumer's commit, the provider shipping alone after a new commit and the W193 catch-up, to the settled plan, has the same exit codes, diagnostics, graph lines and tags, each tag with the subject `release <tag>`, when every build and publication runs on a worker as when nothing is delegated. | `TestExecutionPickUpMatchesALocalRelease` | mapped |
| EXE-60 | yes | The repository being released is the mailbox by default | A worker link with no endpoint, configured or named on the command line, reaches the push URL of the remote the release takes its lock on: a release places its build on a worker that polls that remote, keeps transport commits out of its release history, names its run in the lock taken there and leaves no coordination branch or lock behind; a sweep reaches the same remote, takes no lock and leaves a lock another release holds untouched. A push URL carrying a credential is refused with E225 before any lock or task, naming the link and the remote and never the credential, while an endpoint that names the same repository cleanly runs. A worker that states no endpoint and is started in a checkout of the repository reads its work from the same remote, and one started outside a repository, or whose remote cannot be a mailbox, is refused with E225 naming both remedies. | `TestExecutionReleaseUsesItsOwnRemoteAsTheMailbox`, `TestExecutionSweepUsesItsOwnRemoteAsTheMailbox`, `TestExecutionDefaultMailboxRefusesACredentialCarryingRemote`, `services/dispat/internal/app/execution_test.go::TestWorkerLinksReachTheReleaseRemoteByDefault`, `services/dispat/internal/app/execution_test.go::TestWorkerLinksInAComposedWorkspaceReachTheEntryRemote`, `services/dispat/internal/app/execution_test.go::TestWorkerLinksRefuseARemoteNoMailboxCouldBe`, `services/dispat/internal/app/execution_test.go::TestWorkerLinksNeverEchoACommitRemoteURL`, `TestExecutionWorkerFindsItsMailboxInItsCheckout`, `TestExecutionWorkerStartRefusals`, `services/dispat/internal/app/worker_endpoint_test.go::TestWorkerFindsItsMailboxInTheRepositoryItRunsIn` | mapped |
| EXE-61 | yes | A coordination push is settled by reading, never by pushing again | A push whose answer was lost is read back from the branch: a node's claim that landed runs its task exactly once, a publisher's ready that landed publishes and is recorded without E228, and an assignment whose create applied or never applied is either taken up or revoked and offered again, leaving no coordination branch behind. A result the mailbox refuses with a server rule is reported again without its outputs, so the package fails with E227 and `transfer-refused` long before the task deadline. A lease the remote refused, a server's refusal and an unknown outcome are three different answers, and only a read of the branch turns the last one into either of the others. | `TestExecutionLostClaimResponseRunsTheTaskOnce`, `TestExecutionLostReadyResponsePublishes`, `TestExecutionRefusedResultFailsTheTaskPromptly`, `TestExecutionLostAssignmentCreateLeavesNoBranch`, `TestExecutionLostAuthorizationResponseRetainsExclusion`, `services/dispat/internal/gitx/transport_test.go::TestPushStatusTellsTheThreeOutcomesApart`, `services/dispat/internal/gitx/transport_test.go::TestTransportPushOutcomesAgainstARealRemote`, `services/dispat/internal/execution/mailbox_test.go::TestMailboxSettlesAPushWithNoAnswer`, `services/dispat/internal/execution/mailbox_test.go::TestMailboxCloseSettlesEveryBatch`, `services/dispat/internal/execution/worker_test.go::TestWorkerAdoptsAClaimWhosePushHadNoAnswer`, `services/dispat/internal/execution/worker_test.go::TestWorkerReportsAResultTheMailboxRefused` | mapped |
| EXE-62 | yes | One task's transfer never holds up another task's protocol | A mailbox is never held across a network call: while one task's result push is in flight, another task on the same node reads its publication authorization and publishes, and a poll, a reread and a withdrawal go on. An orchestrator polling a shared mailbox fetches only the branches of its own attempts, and the refs it fetched are removed when their branch closes and when the run closes. | `TestExecutionSlowResultPushDoesNotStarveAnAuthorization`, `services/dispat/internal/execution/mailbox_test.go::TestMailboxDoesNotHoldItselfAcrossATransfer`, `services/dispat/internal/execution/mailbox_test.go::TestMailboxFetchesOnlyWantedBranches`, `services/dispat/internal/execution/mailbox_test.go::TestMailboxCloseRemovesEveryFetchedRef` | mapped |
| EXE-63 | yes | A hung mailbox or an unexpected failure never holds a lock | Revoking and withdrawing an attempt wait at most the cancel wait and a run's close waits a bound of its own, so a mailbox that hangs after preflight costs the run its coordination branches, reported with W244, and the release still gives its lock back and leaves no fetched ref behind. A panic while reading a branch quarantines that branch, one during a probe fails that node's preflight, one in a task reports a failure only when its command never started, and one after a publication was authorized settles it as an unanswered publication, never as a failure. | `TestExecutionHungMailboxStillGivesTheLockBack`, `services/dispat/internal/execution/containment_test.go::TestCloseEndsWithinItsBound`, `services/dispat/internal/execution/containment_test.go::TestWatcherContainsAPanicToItsBranch`, `services/dispat/internal/execution/containment_test.go::TestPreflightContainsAPanicToItsNode`, `services/dispat/internal/execution/containment_test.go::TestWorkerContainsAPanicToItsTask`, `services/dispat/internal/execution/containment_test.go::TestPublicationPanicAfterAuthorizationIsUnknown` | mapped |
| EXE-64 | yes | Cancellation in the closing phase | A SIGINT or SIGTERM during `postAll`, during the `beforeCommit` hook or while the release push is on the wire stops the running hook and every later one, and the release commit and tag still reach the remote; the run exits non-zero and `release.finished` says `interrupted`. | `TestInterruptInTheClosingPhaseStillRecordsWhatPublished` | mapped |
| EXE-65 | yes | A skipped package leaves nothing a retry refuses | In commit mode a package skipped after an earlier stage ran has its folder restored without `revertOnFail`, in a single history and in a fleet source, serialized against its siblings, except the repository root and a folder holding another package; without release commits its edits stay. | `TestHooksCommitModeRestoresASkippedConsumer`, `TestHooksCommitModeRestoresSkippedConsumersConcurrently`, `TestHooksSkippedNestedParentLeavesItsPublishedChildAlone`, `TestHooksSkippedConsumerKeepsItsEditsWithoutReleaseCommits`, `TestPolyrepoCommitModeSourceRestoresASkippedConsumer` | mapped |
| EXE-66 | no | An optional sign stage runs first | A package whose space configures `autoSign` or a sign sequence runs `beforeSign`, `sign` and `postSign` before its version stage and its build, the sign stage waits for providers as a package's first task does and reports itself as `sign` to hooks and webhooks, and a package without that configuration runs exactly the stages it always did. | `TestHooksSignFrameFiresFirst`, `TestOrderSignStageWaitsForProviders`, `TestWebhookSignStageOnlyWhenConfigured` | mapped |

## Publication and recovery

| ID | Critical | Goal | Assertion | Tests | Status |
|---|---|---|---|---|---|
| PUB-01 | yes | One run at a time | The remote `dispat-release-lock` decides who releases: a second run is refused, the lock is taken even for an empty plan, and owned locks are removed on exit. If Git refuses cleanup, the run and completion webhook fail while published packages remain published. | `TestReleaseLockBlocksConcurrentRuns`, `TestReleaseLockClearedWhateverHappens`, `TestReleaseLockTakenEvenWhenNothingToRelease`, `TestReleaseLockCleanupFailureFailsTheCompletedRun` | mapped |
| PUB-02 | yes | Fleet locks unwind cleanly | The control repository's held lock refuses the whole fleet, an uncoordinated source reports E336 before any mutation, contention in a later source preserves that foreign lock while unwinding the locks this run owns, and an interrupted run clears the fleet lock without recording a release. A malformed Git common-directory reply is refused before publication. Cleanup continues in reverse order after one repository's lock cannot be given back; stranded locks fail the run without erasing publication. | `TestPolyrepoReleaseLockGuardsAndCleansSourceWork`, `TestFinalMutationCommonDirectoryRepliesRefuseBeforePublication`, `TestFinalFleetUnlockFailureFailsAfterPublishing` | mapped |
| PUB-03 | yes | The unsafe bypass is explicit | The lock is on by default and only `unsafeDisableLock` or `DISPAT_UNSAFE_DISABLE_LOCK` switches it off; it never becomes an implicit default and never applies to a command other than a release. In an orchestrated fleet only the control configuration's setting counts, and a source's own is reported as ignored while its lock is taken. | `TestReleaseLockKillSwitch`, `TestReleaseLockConfigSwitch`, `TestReleaseLockAppliesOnlyToRelease`, `TestPolyrepoSourceCannotSwitchOffItsOwnLock` | mapped |
| PUB-04 | yes | A published package is never called unpublished | A tag, changelog or release-commit failure after a package published is reported as a critical (E221/E222/E223) with a non-zero exit while the package stays `published` and the packages after it carry on. A fleet GitHub metadata failure preserves the source tag and control checkpoint; release retry does not republish, and the dedicated metadata step repairs the missing GitHub record. | `TestRecordsTagFailureDoesNotUnpublishTheRelease`, `TestRecordsChangelogFailureIsCriticalNotFailure`, `TestRecordsCommitFailureStillTags`, `TestPolyrepoGitHubFailureKeepsNativeRecordForAPIRepair` | mapped |
| PUB-05 | yes | The run says it failed | A successful publication followed by a monorepository release-commit failure exits non-zero and reports the completion webhook `release.finished` as `failed` while preserving the published count. | `TestWebhookReportsMonorepoFinalizeFailure`, `TestWebhookFailedRunKeepsItsExitCode` | mapped |
| PUB-06 | yes | A checkpoint failure preserves what published | E335 after a successful source commit and tag preserves that durable publication, leaves the control gitlink truthful, blocks the dependent leg, reports the final status as failed, and an explicit repair lets a retry keep the provider tag and finish the consumer once. | `TestPolyrepoCheckpointFailurePreservesSourceAndBlocksConsumer`, `TestFinalSourceTagPushFailureKeepsTheAlreadyPushedBranch` | mapped |
| PUB-07 | yes | Dependent work waits for the record | A failed source release commit creates neither the source tag nor the control checkpoint and blocks dependent publication; after the failure is removed and the retained generated state explicitly cleaned, a retry records both repositories. | `TestPolyrepoSourceRecordFailureBlocksConsumerAndRetries` | mapped |
| PUB-08 | yes | Independent success survives a neighbour's failure | An unrelated source failure blocks its own consumer while independent publication stays tagged, and a retry reads that durable tag and publishes only the unfinished work rather than republishing. | `TestPolyrepoPartialReleasePreservesIndependentSuccessOnRetry`, `TestPlanConsumerFailureCatchesUpAfterProviderPublished`, `TestPlanProviderBuildFailureBlocksConsumerThenHeals` | mapped |
| PUB-09 | yes | A checkpoint never outruns its push | E335 on a rejected source branch or tag push may retain local source state but cannot create or push a control checkpoint pointing at a commit the remote does not have. | `TestPolyrepoSourcePushFailureDoesNotAdvanceControl`, `TestPolyrepoCheckpointRefusesASourceRevisionItCannotProve`, `TestPolyrepoDetachedPushRequiresBranch` | mapped |
| PUB-10 | yes | Drift after planning stops publication | A high-water tag added after planning, a baseline tag force-moved after the build, and a control-HEAD mutation that invalidates the provenance of an applicable directive each report E330 before publication and leave the planned tag absent. | `TestPolyrepoBeforeAllTagDriftStopsBeforePublication`, `TestPolyrepoBeforePublishBaselineTagDriftStopsPublication`, `TestPolyrepoBeforePublishControlDirectiveDriftStopsPublication`, `TestFinalRemainingRepositoryInputClosureCrossesGroupsAndDependencies`, `TestFleetPlanningHeadMismatchStopsBeforePublication` | mapped |
| PUB-11 | yes | Drift before planning stops the run | An uninitialized submodule, a checkout whose HEAD differs from its control gitlink, a shallow source, a participant whose remote cannot be proved and a participant that pushes from a checkout behind its remote branch all fail before planning rather than producing a plan about the wrong revision; a detached participant has no branch to compare and still releases its tag. | `TestPolyrepoRefusesUninitializedPinnedMismatchAndShallowSources`, `TestPolyrepoFixedRideGuardsSourceHistoryWithoutDependency`, `TestPolyrepoControlBehindItsRemoteRefusesBeforePlanning`, `TestPolyrepoDetachedSourcePinnedBehindItsBranchStillReleases`, `TestFleetSourceRemoteReadFailureStopsBeforePublicationAndCanRetry` | mapped |
| PUB-12 | yes | An ambiguous external write is verified, not retried | A GitHub create the API persisted whose response was lost is reported as E222 with a non-zero exit, independent work continues, and the durable tag makes the rerun a no-op without a second POST. | `TestReleaseReconcilesGithubCreateWhoseResponseWasLost` | mapped |
| PUB-13 | yes | Foreign refs are never overwritten | A release tag that already sits at another commit is reported (E221) and left where it is, rather than force-moved over the copy the remote carries. | `TestRecordsTagAtAnotherCommitIsLeftAlone`, `TestRecordsForceRewritesAnUnreachableTag`, `TestReleaseRecordsCreateOnlyPush` | mapped |
| PUB-14 | no | A rerun converges | A record the destination already carries is a reported skip rather than an error: W224 for a GitHub release, W226 for a changelog entry, so a repeated step command and the release that follows both converge. | `TestRecordsGitHubReleaseExistsIsASkip`, `TestRecordsAdoptionConverges`, `TestRecordsStandaloneChangelogPreservesThePreamble` | mapped |
| PUB-15 | no | Work that landed during the run | A commit pushed from elsewhere while the build ran reaches the finalize push as a rejection, and the release merges itself with what landed (W242) rather than rewriting it. | `TestReleaseMergesWhatLandedDuringTheRun`, `TestFinalConflictInspectionFailureAbortsBeforeSettlement`, `TestFinalRecoveryAbortFailureReportsTheRetainedObstacle`, `TestFinalConflictQuarantineLookupRefusalsPreserveBothSides` | mapped |
| PUB-16 | no | Records are written where they were promised | Release records reach the destinations their channels admit and no others, and a package's own policy replaces the inherited one rather than extending it. | `TestRecordsChannelsHoldPrereleasesBack`, `TestChannelsFilterRecordLines`, `TestOverridesPerPackageRecords` | mapped |
| PUB-17 | yes | A fleet records what it incorporated before it publishes | Every repository on the route records the revision of the next hop before the consumer publishes, so the commit its tag names carries the evidence, and a second run of the same command records and publishes nothing. A provider whose record fails blocks its consumer, a consumer that sat out its provider's later release catches up, and a provider retired from the composed fleet creates no debt. | `TestChoreographySettlesTheProviderRevisionBeforePublishing`, `TestChoreographyRecordsEveryPeerLocallyAndConverges`, `TestChoreographyMissingProviderTagBlocksConsumer`, `TestChoreographyCatchesUpAfterProviderOnlyRetry`, `TestChoreographyRemovedProviderCreatesNoDebt`, `TestChoreographyPushesEveryRepositoryItRecorded`, `TestChoreographyNestedCommitStepRecordsTheEvidence`, `TestChoreographyConvergesAfterARejectedSettlementPush` | mapped |
| PUB-18 | yes | Evidence that cannot be complete is refused before publication | A route whose repository cannot record its hop refuses the package before anything external happens, and a consumer that records nothing says so instead of publishing evidence that stops halfway. | `TestChoreographyRefusesAPackageWhoseRouteCannotRecord`, `TestChoreographyRecordsNothingForATagOnlyConsumer`, `TestChoreographyRefusesToPushASettlementFromADetachedCheckout`, `TestChoreographyRefusesAPinItsTargetHasNotPushed`, `TestChoreographyVerifiesADetachedPeerAgainstTheRoster` | mapped |
| PUB-19 | yes | A fleet's repositories stay each other's equals | One peer's unsafe lock setting is its own, the entry's run hooks fire once, a rollback restores through the repository that owns the folder, and two consumers settling overlapping routes neither deadlock nor serialize. | `TestChoreographyBypassesTheLockPerRepository`, `TestChoreographyLocksEveryRepositoryInNameOrder`, `TestChoreographyRunsTheEntryHooksOnce`, `TestRevertOnFailRestoresThroughTheOwningRepository`, `TestChoreographyConcurrentConsumersSettleInLaneOrder` | mapped |
| PUB-20 | yes | A release plans from the records of the store it writes to | Under its locks and before the plan, a write-capable run compares every participating repository's remote release records with the ones it plans from: a record it lacks on a commit its planned head reaches is E196 and a version named at two commits is E191, both refused before any script, with nothing repaired, fetched or moved. A run that pushes nothing, and one with `commit.verify: false`, read no records at all. | `TestReleaseRecordsRefuseACloneWithoutTheRemoteTags`, `TestReleaseRecordsNeverMoveAPublishedTag`, `TestReleaseRecordsRefuseATagAtAnotherCommit`, `TestReleaseRecordsInAComposedWorkspace`, `TestReleaseRecordsRespectVerifyOffAndNoPush`, `TestReleaseRecordsIgnoreATagWithNoVersionInIt`, `TestFleetSourceRemoteReadFailureStopsBeforePublicationAndCanRetry` | mapped |
| PUB-21 | yes | A release record is created on the remote and never replaced | Every release tag is written create-only: a name the remote already holds at this release's commit is the retry of a lost write and converges, a name it holds at another commit is reported (E221) and left exactly where it is while the package stays `published`, and only an alias declared `moving` is replaced. | `TestReleaseRecordsCreateOnlyPush`, `TestReleaseRecordsAliasTagsStillMove`, `TestReleaseRecordsUnwritableStoreKeepsThePublication`, `TestReleaseRecordsUnreadableCheckoutRefusesTheRun` | mapped |
| PUB-22 | yes | A consumer never publishes behind a provider that did not | A consumer's publish waits for its providers' publishes under every relation, and a provider that failed or was skipped under a blocking relation skips its consumers whatever release reason they carry of their own, including one that has already built: `W194` names the provider, nothing is tagged, `onSkip` runs and `revertOnFail` rolls the finished build back out of the folder. `none` and `publish` block by default; only the relaxed `{build: none, isBlocking: false}` lets such a consumer release its own work. | `TestStageRelationBlockingSkipsAConsumerWithItsOwnChanges`, `TestStageRelationBuildsOverlapWhilePublishesFollowTheProvider`, `TestOrderProviderFailureSkipsTheWaitingConsumer` | mapped |
| PUB-23 | yes | The order and the blocking hold through packages that are not releasing | Publication order and the W194 closure are taken over the whole workspace graph and restricted to the plan afterwards, so a provider a consumer reaches only through a package with nothing to release still publishes first, and one whose publish failed still skips that consumer, named by `blockedBy`, unless it carries a fresh release reason of its own under a relation that does not block. | `TestPublishOrderHoldsThroughAPackageThatIsNotReleasing`, `TestPublishOrderBlocksAConsumerBehindAFailedProviderItDoesNotNameDirectly` | mapped |
| PUB-24 | yes | A manifest never names a version nobody published | A consumer that proceeds past a provider which died after its version stage publishes the provider's baseline, because its reconciliation is redone against the providers that are still alive; a failed native or scripted reconciliation withholds publication until repaired; one whose build already embedded the planned version is blocked with W194 naming the embedding rather than rebuilt behind the operator's back. | `TestAdmissionCatchesUpAConsumerThatProceededOnItsOwn`, `TestAdmissionBlocksAProceedingConsumerWhoseBuildEmbeddedThePlannedVersion`, `TestAdmissionFailedReconciliationWithholdsConsumerPublication` | mapped |
| PUB-25 | yes | A GitHub response must finish within its request timeout | A GitHub API that sends a successful status and then stops before the body is complete cannot hold the command past its request timeout or turn partial JSON into a completed record. | `TestRecordsGithubHeadersWithoutBodyRespectTheRequestTimeout` | mapped |
| PUB-26 | yes | A publication starts only under the lock | Every release that took a lock, local or distributed, reads it back from the remote before each publish command through one ownership gate: another object or none withholds the publication with E336 before its command starts and leaves the other run's lock where it is, a read that fails is retried and bounded, and `commit.verify: false` skips the read with one warning while a distributed release with that setting is refused with E225. | `TestReleaseLockReplacedBeforePublishWithholdsThePublication`, `TestReleaseLockVerifyOffSkipsTheLockRead`, `services/dispat/internal/app/execution_test.go::TestLocalFleetReleaseChecksTheWholeLockSet`, `services/dispat/internal/app/execution_test.go::TestCheckExecutionEntryRefusals` | mapped |
| PUB-27 | yes | A lost lock answer is settled from the remote | A lock push or delete that reports a failure is read back before it is believed: a push the remote carries is this run's, a push no read can establish is removed under a lease on its own object and refused with E336 naming the attempt, a delete that left no lock or left another run's lock is a clean release, and a cleanup that meets another run's lock leaves it in place without telling anybody to delete it. | `TestReleaseLockRecoversItsOwnAcceptedPushAfterAResponseFailure`, `TestReleaseLockRemovesAPushWhoseOutcomeCannotBeRead`, `TestReleaseLockLostDeleteResponseIsARelease`, `TestReleaseLockCleanupPreservesAReplacedLock` | mapped |
| PUB-28 | yes | A remote credential never reaches a log | A remote is named in every log line and error with its user information, query and fragment taken out, whether it is a named remote or the URL itself, and whether the run is refused upfront or its push fails. | `TestRecordsNeverLogARemoteCredential` | mapped |
| PUB-29 | yes | The release commit never records a version that did not publish | When a package whose version or `syncLock` stage ran does not publish, a single history restores the shared `commit.include` paths and the unpublished packages' tracked files and runs the published packages' `syncLock` again with a settled environment, so the release commit's shared lock file lists only published versions; a regeneration that is interrupted leaves the paths at HEAD and out of the commit with `E223`, while the published packages are still committed and tagged. | `TestEdgeReleaseCommitLockOmitsASkippedConsumer`, `TestEdgeReleaseCommitLockOmitsAFailedWriter`, `TestEdgeReleaseCommitLockLeftAtHeadWhenResyncIsInterrupted` | mapped |

## Distribution

| ID | Critical | Goal | Assertion | Tests | Status |
|---|---|---|---|---|---|
| DST-01 | no | Self-update verifies before it replaces | The downloaded binary is checked against the size and checksum the release published and run once before it is moved into place. A second successful install retains only the binary it replaced; a failed swap preserves both the current binary and prior backup. Unsafe or unparkable backups block replacement; a backup's place that something other than a file occupies is named with its remedy before anything is downloaded. A rollback copy an interrupted update left parked is recovered by the next rollback. If the running executable path disappears during validation, an older rollback copy survives and is reported; without one, no rollback is advertised. | `TestSelfUpdateReplacesTheRunningBinary`, `TestSelfUpdateSecondInstallKeepsOnlyTheVersionItReplaced`, `TestSelfUpdateFailedSecondInstallPreservesBothBinaries`, `TestSelfUpdateRefusesAnUnsafePreviousBackup`, `TestSelfUpdateRollbackRecoversABackupACrashLeftParked`, `TestSelfUpdateCannotParkPreviousBackup`, `TestSelfUpdateKeepsPreviousBackupWhenCurrentDisappears`, `TestSelfUpdateWithoutPreviousBackupReportsNoRollback`, `TestSelfUpdateOverTLS`, `TestSelfUpdateBackupExpiresOnItsOwn` | mapped |
| DST-02 | no | Self-update reports before it acts | `--check` reports what the same invocation would do and exits 1 when there is something to install, and `--release` installs one named version including a downgrade. | `tests/integration/selfupdate_test.go::TestSelfUpdateCheckShowsWhatWouldArrive`, `TestSelfUpdateInstallsANamedVersion`, `TestSelfUpdateAndPrereleases` | mapped |
| DST-03 | no | Installing a tool is reversible | `dispat install` refuses a destination it must not replace, keeps what it replaced, and restores it on `--rollback`. | `tests/integration/install_test.go::TestInstallRefusesADestinationItMustNotReplace`, `TestInstallKeepsAndRestoresWhatItReplaced`, `TestInstallIsIdempotent` | mapped |
| DST-04 | no | The public manifest API round-trips | The public scanner and writer read every supported manifest family, preserve unrelated author text through a rewrite, and leave input byte-identical when they refuse it, including a real filesystem failure after a change is prepared; the CLI path also refuses a short temporary write before replacing a large manifest. Malformed local-link syntax is not treated as an actionable redirect. A package.json that carries the same local redirect in several manager override maps can drop or repoint every local occurrence while preserving registry overrides. Cargo dotted dependency keys update the exact key the scanner recognized, without confusing a literal dot in a quoted name with a dotted path; multiline and inline string contents cannot masquerade as declarations across TOML formats. | `TestPublicAPIScannerWriterScannerLifecycle`, `TestPublicAPIExtendedManifestLifecycles`, `TestPublicAPIRefusalsPreserveInputsAndBoundResources`, `TestPublicAPIManifestReadersRefuseSpecialFiles`, `TestPublicAPIWriterRefusesAManifestDirectory`, `TestPublicAPIWriterKeepsOriginalBytesWhenItsDirectoryCannotBeWrittenTo`, `TestPublicAPIAtomicEditsSurvivePartialDiskWrites`, `TestAutoVersionManifestSurvivesAPartialDiskWrite`, `TestPublicAPIWriterDoesNotInventALinkFromBrokenPubspecFlowSyntax`, `TestPublicAPIWriterDropsDuplicateNpmLinksAcrossOverrideFields`, `TestPublicAPIWriterKeepsRegistryOverrideBesideDuplicateLocalLink`, `TestPublicAPIWriterUpdatesCargoDottedDependencyVersion`, `TestPublicAPIWriterIgnoresTOMLStringsThatResembleCargoDeclarations`, `TestPublicAPIWriterIgnoresMultilineTOMLDecoysAcrossFormats` | mapped |
| DST-05 | no | Manifest commands need nothing else | `dispat scanner`, `dispat writer` and `dispat replacer` run with no config file, no commit and no plan, and their outcomes reach the process exit code. | `TestManifestsScannerStrictGatesBrokenManifests`, `TestManifestsWriterOutcomesReachTheExitCode`, `TestManifestsReplacerOutcomesReachTheExitCode` | mapped |
| DST-06 | no | The npm distribution | The npm packages publish against the CLI release they wrap: the first release uses an exact version, prerelease and graduated binaries stay pinned, and a failed publication retries its own leg without republishing the rest. | `TestNPMDistributionFirstReleaseUsesExactVersionAndChoreCorrections`, `TestNPMDistributionPinsPrereleaseAndGraduatedBinaries`, `TestNPMDistributionWaitsForPublicationAndRetriesItsOwnFailure` | mapped |
| DST-07 | no | Composed tool manifests | An Aqua registry import is read, written and version-pinned through its composed manifest without evaluating dynamic entries; malformed or oversized imported siblings leave valid manifests discoverable, and a refused write preserves the writes that completed. | `TestAquaDemoFixtureThroughManifestCommands`, `TestAquaComputeAndAutoversionUseQualifiedOwnership`, `TestAquaWriterBatchKeepsCompletedWriteWhenSymlinkRefuses`, `TestPublicAPIScannerKeepsValidAquaImportsBesideRejectedSiblings` | mapped |

## Observability

| ID | Critical | Goal | Assertion | Tests | Status |
|---|---|---|---|---|---|
| OBS-01 | yes | Credentials stay out of the wire and the log | A token named for one host is never sent to another, an invocation without a token stays on the public URL, and a private repository install requires the token to be named rather than guessed. | `TestInstallKeepsTheTokenAwayFromAnotherHost`, `TestInstallWithoutATokenStaysOnThePublicURL`, `TestSelfUpdateWithoutATokenStaysOnThePublicURL`, `TestInstallFromAPrivateRepositoryNeedsTheTokenNamed` | mapped |
| OBS-02 | no | The JSON event stream | `--log-format json` emits one machine-readable event per diagnostic and per outcome, and a command's own output does not contaminate it. | `TestInstallEventsReachTheJSONStream`, `TestInstallPipeOutputStaysOutOfTheJSONStream`, `tests/integration/selfupdate_test.go::TestSelfUpdateNotesReachTheJSONStream` | mapped |
| OBS-03 | no | Reporting never gates the run | A webhook endpoint that answers 500, refuses the connection, hangs before headers or stalls after a 200 header costs the run nothing beyond W239; an incomplete body is never counted as delivered, and a full queue drops excess deliveries without delaying or retrying publication. | `TestWebhookFailingEndpointNeverAffectsTheRelease`, `TestWebhookUnreachableEndpointNeverAffectsTheRelease`, `TestWebhookSlowEndpointIsBounded`, `TestWebhookHeadersWithoutBodyAreBounded`, `TestWebhookFullQueueDropsDeliveriesWithoutRetryingPublication` | mapped |
| OBS-04 | no | A delivery can be trusted | Every delivery carries the configured method and headers and, with `secretEnv` set, an `X-Dispat-Signature` that verifies as the standard `sha256=` HMAC over the exact body bytes. | `TestWebhookSignature`, `TestWebhookHeadersAndMethod`, `TestWebhookCustomFormat` | mapped |
| OBS-05 | no | A run is subscribable in order | `release.started` opens with the plan snapshot, each package's stages bracket exactly, and `release.finished` closes with the counts; a run refused before execution delivers nothing at all. | `TestWebhookHappyPathSequence`, `TestWebhookRefusedRunEmitsNothing`, `TestWebhookScriptProgressTrigger` | mapped |
| OBS-06 | no | Diagnosing one message | `dispat diagnostics` parses exactly one supplied message and reports every warning and error without Git, a repository or the local configuration, and warnings stay visible under history-quiet settings. | `TestDiagnosticsDefaultsNeedNeitherGitNorConfig`, `TestDiagnosticsWarningsRemainVisibleAsJSON`, `TestDiagnosticsSeparatesUsageAndConfigErrors` | mapped |
| OBS-07 | no | Seeing the plan before running it | `dispat status` prints the plan a release with the same flags would execute and `dispat preview` prints the pending notes under each entry format, both without building, tagging or writing anything. | `TestFilterStatusSelects`, `TestCommandsPreviewRecordBodies`, `TestChannelsPreviewShowsBothBodies` | mapped |
| OBS-08 | no | An entry always states its cause | A release with nothing for its notes to group states why in the changelog entry and in the GitHub body instead of rendering an empty record. | `TestEntryBodyOfACancelledOutRelease`, `TestRecordsCustomNoChangesText`, `TestRecordsNoChangesTextThatExpandsToNothingFallsBack` | mapped |
| OBS-09 | no | The commands as a pipeline uses them | One workspace walked through the commands CI leans on, each asserting its observable outcome, so a change to any one of them fails here rather than in somebody's pipeline. | `TestSmokeKeyFeatures`, `TestSmokeReleaseCycles` | mapped |
| OBS-10 | no | Planning workload is observable | Debug reports numeric history-work counts without changing the selected packages, versions or reasons; ordinary info output has no workload event. | `TestFinalPlanningWorkloadIsDebugOnlyAndPreservesThePlan` | mapped |
| OBS-11 | no | A distributed run says which machine wrote each line | Every line a process taking part in distributed execution writes after reading its configuration names that process with a role and a node, a line about another node names it in `worker` instead, a build script's relayed output and a nested dispat invocation on a node carry the node's identity whatever the transported checkout states, and a repository with no `execution` object writes the lines it always wrote. | `TestExecutionEveryLogLineNamesItsNode`, `TestExecutionAbsentAddsNoIdentityFields` | mapped |
| OBS-12 | no | A webhook event names its sender and the node it is about | Every delivery of a distributed run carries the sending role and node, which is a non-empty name even when the file states none; the events of a delegated stage and the package outcome behind it name the node that ran the work while the events of a stage the orchestrator kept name none; a `format` template may render all three; a build script may raise its own event from the node it was placed on, signed and headed from that node's environment; and a run with no `execution` object delivers the payloads it always delivered. | `TestExecutionWebhooksNameTheirSender`, `TestExecutionTriggerFromAWorkerNamesTheWorker`, `TestExecutionAbsentAddsNoIdentityFields` | mapped |

## Gates on this matrix

The traceability gate is `go run ./tools/testreport testplan .`, wired into CI as the `repo-checks` target of
`Dockerfile.gotest` and runnable locally through `scripts/check-test-plan.sh`. It fails on a reference to a test that
does not exist, on an ambiguous bare name that more than one validated module declares, on an integration test with no
goal in the [test plan](./test-plan.md), on a row whose status its references do not support, and on a mapped or
critical percentage below the thresholds the gate is given. `scripts/check-test-plan-test.sh` runs the gate's own
tests, which are Go tests of the command in `tools/testreport/testplan_test.go`.

Reference integrity is not the same claim as coverage. A named assertion is evidence only once the suite that contains
it has run: `testreport coverage -minimum-integration 95 -minimum-total 95` is the separate gate over the profiles, and
it rejects missing, stale, mixed-commit, unexpected and orphan evidence before reporting a number.

The environmental limits stand. Disposable local Git repositories and deterministic HTTP fixtures do not exhaust every
hosting provider failure, filesystem, credential helper or network partition. The release experiments supplement those
fixtures with selected partial-publication protocols, and a recorded failure there stays a failure rather than being
rewritten as a successful observation.
